# Feature Implementation Status

_Audit of all existing code, 306 features across 10 domains._

| Domain | ✅ Complete | 🟡 Partial | 🔲 Stub | 📋 Planned | ❌ Broken |
|---|---|---|---|---|---|
| **messaging** | 32 | 12 | 6 | 0 | 2 |
| **llm-providers** | 14 | 8 | 3 | 1 | 2 |
| **memory** | 16 | 4 | 1 | 3 | 0 |
| **mcp-tools** | 6 | 9 | 2 | 0 | 8 |
| **bots-config** | 30 | 5 | 1 | 0 | 1 |
| **pipeline** | 14 | 3 | 0 | 0 | 5 |
| **monitoring** | 11 | 7 | 3 | 3 | 1 |
| **auth-security** | 16 | 8 | 2 | 3 | 1 |
| **persistence** | 20 | 7 | 3 | 1 | 5 |
| **webui-infra** | 19 | 4 | 2 | 0 | 2 |
| **TOTAL** | **178** | **67** | **23** | **11** | **27** |


## messaging

| Feature | Status | Evidence |
|---|---|---|
| IMessengerService / IMessage abstraction layer | ✅ complete | src/message/interfaces/IMessengerService.ts and IMessage.ts define a full provider-agnostic contract (send, fetch, typing, threads, multi-bot via getDelegatedServices, resolveAgentContext, scoreChannel, isConnected). Discord/Slack/Mattermost services all implement it concretely. |
| Discord: send message to channel | ✅ complete | packages/message-discord/src/managers/DiscordMessageSender.ts:60 real channels.fetch + channel.send with payload, input validation, XSS pattern rejection, per-channel rate limiting (lines 292-317), WS message-flow events. |
| Discord: receive/handle incoming messages | ✅ complete | packages/message-discord/src/managers/DiscordEventHandler.ts:101 attaches messageCreate listener per bot, wraps in DiscordMessage with replied-message metadata, fetches history, invokes handler; honors MESSAGE_IGNORE_BOTS. |
| Discord: thread replies | ✅ complete | DiscordMessageSender.ts:173-187 fetches threadId, validates isThread(), sends via thread.send(payload); reply-to via payload.reply.messageReference (lines 168-171). |
| Discord: reply-to-message (message reference) | ✅ complete | DiscordMessageSender.ts:169 sets payload.reply={messageReference:replyToMessageId,failIfNotExists:false}; DiscordMessage.ts:365 isReplyToBot and metadata.replyTo populated. |
| Discord: typing indicator | ✅ complete | DiscordMessageSender.ts:20 sendTyping resolves bot by snowflake/name and calls channel/thread.sendTyping(); also startTypingIndicator.ts refreshes every 15s; event handler tracks others' typingStart (DiscordEventHandler.ts:86). |
| Discord: multi-bot instances under one service | ✅ complete | DiscordBotManager.ts manages a Bot[] pool with per-bot Client; DiscordService.getDelegatedServices() (DiscordService.ts:594) exposes per-bot wrappers; sender selects bot by snowflake id or name. |
| Discord: fetch channel history | ✅ complete | DiscordService.fetchMessages (DiscordService.ts:278) calls channel.messages.fetch with DISCORD_MESSAGE_HISTORY_LIMIT cap, reverses to oldest-first, wraps in DiscordMessage. |
| Discord: channel list / topic / owner introspection | ✅ complete | DiscordService.getChannels (line 109), getChannelTopic (323), getChannelOwnerId (345) query discord.js channel/guild caches and API. |
| Discord: runtime add/disconnect bot + reconnection | ✅ complete | DiscordService.addBot (line 211) logs in new client; DiscordProvider.addBot/reload (src/providers/DiscordProvider.ts:75,144) wraps each in ReconnectionManager with health checks (client.isReady). |
| Discord: presence/model activity status | ✅ complete | DiscordService.setModelActivity (line 510) selects bot and calls client.user.setActivity(modelId,{type:0}). |
| Discord: message editing / attachments detection | ✅ complete | DiscordMessage.setText (line 133) edits via message.edit when editable; hasAttachments (line 458), getReferencedMessage (438), getUserMentions/getChannelUsers real Collection parsing. |
| Slack: send message to channel | ✅ complete | SlackService.sendMessageToChannel (SlackService.ts:629) -> SlackMessageIO.sendMessageToChannel (modules/ISlackMessageIO.ts:300) real chat.postMessage with per-bot send queue, retry/backoff, block support. |
| Slack: receive incoming messages (Socket Mode) | ✅ complete | SlackBotManager.startListening (SlackBotManager.ts:98) wires SocketModeClient 'message' events, dedupes by event_ts, filters bot/self messages, invokes messageHandler; SlackService.setMessageHandler (line 466) builds the LLM pipeline. |
| Slack: receive via HTTP Events API endpoint | ✅ complete | SlackEventProcessor.handleActionRequest (SlackEventProcessor.ts:26) handles url_verification challenge, event_callback message events, dedup, deleted-message tracking; routes registered via SlackEventBus.registerBotRoutes in SlackService.initialize (line 424). |
| Slack: thread replies | ✅ complete | ISlackMessageIO.ts:343 resolvedThreadId=threadId||replyToMessageId sets options.thread_ts; SlackService handler computes threadTs=thread_ts||ts (SlackService.ts:527) and replies in-thread. |
| Slack: multi-bot instances | ✅ complete | SlackService keeps per-bot Maps of managers/processors (SlackService.ts:92-103), initializeBotInstance per BOTS_* config, getDelegatedServices (1055) and getBotNames; supports DM-only secondary bots in SlackBotManager (line 224). |
| Slack: fetch channel history | ✅ complete | SlackService.fetchMessages (line 741) retries then ISlackMessageIO.fetchMessages (line 447) calls conversations.history and maps to SlackMessage. |
| Slack: channels list / topic / owner introspection | ✅ complete | SlackService.getChannels (369) conversations.list; getChannelTopic (862) conversations.info purpose/topic; getChannelOwnerId (885) returns channel.creator. |
| Slack: rich blocks / response formatting | ✅ complete | sendMessageToChannel accepts KnownBlock[] (SlackService.ts:635); SlackMessageProcessor.processResponse returns {text,blocks} consumed in handler (line 577-590). |
| Slack: signature verification (security) | ✅ complete | SlackSignatureVerifier (SlackSignatureVerifier.ts, tested in SlackSignatureVerifier.test.ts) registered per bot; verifySlackSignature also in webhook routes. |
| Slack: send retry / rate-limit backoff | ✅ complete | SlackService.retry helper (line 55) + ISlackMessageIO.sendWithRetry/updateWithRetry/deleteWithRetry (line 75+) handle 429/5xx with exponential backoff and retry_after, bail on channel_not_found/missing_scope. |
| Slack: runtime add/remove bot | ✅ complete | SlackService.addBot (line 209) registers routes + initializes via facade; removeBot (1236) disconnects socket/RTM and clears maps. |
| Mattermost: send message to channel | ✅ complete | MattermostService.sendMessageToChannel (MattermostService.ts:131) with retry/backoff -> MattermostClient.postMessage (mattermostClient.ts:81) real POST /posts, root_id for threads; channel-name->id resolution with cache (resolveChannelId line 177). |
| Mattermost: thread replies | ✅ complete | sendMessageToChannel passes rootId=threadId||replyToMessageId as root_id (MattermostService.ts:170); MattermostClient.postMessage forwards root_id. |
| Mattermost: fetch channel history | ✅ complete | MattermostService.fetchMessages (line 304) -> client.getChannelPosts (mattermostClient.ts:101) GET /channels/{id}/posts, resolves usernames/is_bot, wraps in MattermostMessage, reversed. |
| Webhook: Replicate/prediction route -> channel announcement | ✅ complete | src/webhook/routes/webhookRoutes.ts:77 configureWebhookRoutes registers POST /webhook with token+IP+validation, then calls messageService.sendPublicAnnouncement on the real (Discord/Slack) service; POST /webhook/slack verifies signature. |
| Telegram: send message | ✅ complete | src/providers/TelegramProvider.ts:332 real fetch to api.telegram.org/bot{token}/sendMessage with HTML parse_mode; sendMessageToChannel (387) prepends agent name. |
| Telegram: forum/chat owner lookup | ✅ complete | TelegramProvider.getForumOwner (line 432) calls getChatAdministrators and returns the creator's user id. |
| Reconnection manager (exponential backoff + health checks) | ✅ complete | src/providers/ReconnectionManager.ts implements state machine, exponential backoff with crypto jitter, maxRetries, periodic healthCheck that triggers onDisconnected; used by Discord/Telegram/Mattermost providers. |
| Bot-to-bot / unsolicited message controls | ✅ complete | DiscordService.initialize (DiscordService.ts:163-191) reads MESSAGE_IGNORE_BOTS, MESSAGE_ALLOW_BOT_TO_BOT_UNADDRESSED, MESSAGE_ONLY_WHEN_SPOKEN_TO, grace window and prints config banner; event handler enforces ignoreBots filter. |
| Per-message WebSocket monitoring (message flow events) | ✅ complete | Discord sender/event handler, Slack messageIO/handler, Mattermost service all call webSocketService.recordMessageFlow for incoming/outgoing with provider/botName/contentLength/status. |
| Discord: voice channel join/leave | 🟡 partial | DiscordService.joinVoiceChannel/leaveVoiceChannel (line 572-588) delegate to VoiceChannelManager, but packages/message-discord/src/voice/voiceChannelManager.ts is an explicit no-op stub ('original voice module sources were removed', joinChannel returns null). |
| Slack: typing indicator (placeholder/RTM) | 🟡 partial | SlackService.sendTyping (line 698): RTM client supports real sendTyping, but Socket/Web API path posts a fake '_is typing..._' placeholder message (ISlackMessageIO.sendTypingPlaceholder:236) later updated/deleted; not a native typing event. Gated by SLACK_FAKE_TYPING. |
| Slack: interactive actions / modals / buttons | 🟡 partial | SlackInteractiveActions.ts + SlackInteractiveHandler wired through SlackEventBus; handlers exist (sendAskQuestionModal, handleButtonClick) but actions are canned domain content (course info/booking/study resources) rather than generic — purpose-specific demo-style handlers. |
| Slack: model activity / status | 🟡 partial | SlackService.setModelActivity (line 1020) sets users.profile status but only when SLACK_ENABLE_STATUS_UPDATES==='true' (off by default) and requires user-token scopes Slack bots usually lack. |
| Mattermost: typing indicator | 🟡 partial | MattermostService.sendTyping (line 562) -> MattermostClient.sendTyping (mattermostClient.ts:251) POSTs /users/{id}/typing best-effort; note this is a REST call (not the WS typing channel) and silently swallows errors. |
| Mattermost: multi-bot instances | 🟡 partial | MattermostService maintains clients Map keyed by bot name (line 27) and getDelegatedServices (656); but since receive is non-functional, multi-bot is effectively send-only. |
| Mattermost: channel list / topic / owner | 🟡 partial | getChannels (line 632) and getChannelTopic (547) work via client.getChannels/getChannelInfo; getChannelOwnerId (608) always returns '' (commented: Mattermost doesn't track creators by default). |
| Webhook: incoming webhook handling (push) | 🟡 partial | WebhookService.handleIncomingWebhook (packages/message-webhook/src/WebhookService.ts:68) builds a mock IMessage from generic/Slack payloads and invokes the handler — real logic, but it is not wired into getMessengerProvider (only discord/slack/mattermost are loaded, src/message/management/getMesseng |
| Webhook: provider registration | 🟡 partial | src/providers/WebhookProvider.ts wraps the stub WebhookService for ProviderRegistry, so a registered webhook 'messenger' can be created but its send is a stub and it is not part of the main messenger load list. |
| Telegram: status / health / reconnection / addBot | 🟡 partial | getStatus/healthCheck call getMe (lines 95,508); addBot/reload persist to messengers.json and wrap getMe checks in ReconnectionManager; but Telegram is not included in getMessengerProvider's loaded provider list (only discord/slack/mattermost), so not part of live messaging. |
| Channel routing / prioritization (scoreChannel) | 🟡 partial | DiscordMessageSender.sendMessageToChannel picks best channel via channelRouter.pickBestChannel when MESSAGE_CHANNEL_ROUTER_ENABLED (DiscordMessageSender.ts:122); Slack/Mattermost expose scoreChannel via ChannelRouter.computeScore but gate to 0 when flag off; feature is off by default and Slack/Matte |
| Provider selection / multi-provider bootstrap | 🟡 partial | src/message/management/getMessengerProvider.ts loads message-{discord,slack,mattermost} via PluginLoader filtered by MESSAGE_PROVIDER; Telegram and Webhook providers exist but are NOT in the loaded set, defaulting to Slack when nothing configured. |
| Discord: slash-command/interaction handling | 🔲 stub | DiscordEventHandler.ts:152 attachInteractionListeners only matches 'speckit specify' and replies 'handler not available in decoupled mode'; no real command dispatch wired. |
| Discord: voice speech-to-text | 🔲 stub | packages/message-discord/src/voice/speechToText.ts:4 returns '' always; transcribeAudio aliases the same stub. |
| Slack: reactions handling | 🔲 stub | No reaction send/add API used; SlackMessage.sanitizeMessageData (line 243) and SlackMessageProcessor map reaction metadata for history context only — bot cannot add reactions and reaction_added events are not handled. |
| Mattermost: model activity status | 🔲 stub | MattermostService.setModelActivity (line 577) is a no-op (void params; return). |
| Webhook: outgoing send (package WebhookService) | 🔲 stub | packages/message-webhook/src/WebhookService.ts:25 sendMessageToChannel returns `webhook-msg-${Date.now()}` with no HTTP POST; comment says 'This would typically involve an HTTP POST'. getMessagesFromChannel returns []. |
| Telegram: receive messages / history | 🔲 stub | TelegramProvider.getMessages (line 377) returns [] with debug 'Telegram Bot API does not support message history retrieval'; no update/long-poll/webhook listener exists, so no incoming handling. |
| Mattermost: receive/handle incoming messages | ❌ broken | MattermostService.setMessageHandler (line 119) only logs and stores nothing; MattermostClient has NO websocket/event subscription (grep for WebSocket finds only a config schema label in schema.ts:60 and WS metrics emit). Bots can send but never receive, so they can never respond to users. |
| Mattermost: runtime addBot via provider | ❌ broken | MattermostProvider.addBot (src/providers/MattermostProvider.ts:71) sets up a ReconnectionManager whose connectFn throws 'Method not implemented.' and then itself throws 'Method not implemented.' (lines 83,99). reload() (166) re-calls service.initialize per instance as a workaround. |

## llm-providers

| Feature | Status | Evidence |
|---|---|---|
| OpenAI chat completion | ✅ complete | packages/llm-openai/src/openAiProvider.ts:63-182 — real openai.chat.completions.create with system prompt, history mapping, retries (MAX_RETRIES=3, exp backoff), circuit breaker, withTimeout, temperature boost & maxTokens/model overrides via metadata. Wired through getLlmProvider/taskLlmRouter. |
| OpenAI text (non-chat) completion | ✅ complete | packages/llm-openai/src/openAiProvider.ts:184-216 — openai.completions.create on legacy /completions endpoint, hardcoded max_tokens:150, circuit breaker + timeout. Works but uses the deprecated completions API and ignores temperature/org config used by chat path. |
| OpenAI embeddings | ✅ complete | packages/llm-openai/src/openAiProvider.ts:218-241 — generateEmbedding() calls openai.embeddings.create with text-embedding-3-small, circuit breaker + 30s timeout. Real implementation (extra capability beyond ILlmProvider). |
| OpenAI default-provider fallback wiring | ✅ complete | src/llm/getLlmProvider.ts:192-210 — when no profile/configured/legacy provider resolves, loads llm-openai as 'default-openai'. Provider cache + token-counting wrapper applied. Confirmed end-to-end. |
| Flowise chat completion (REST + SDK) | ✅ complete | packages/llm-flowise/src/flowiseProvider.ts:26-61 picks REST (flowiseRestClient.ts getFlowiseResponse, with per-channel chatId session persistence) or SDK (flowiseSdkClient.ts). Stateful (manages own history). Requires channelId in metadata or returns a fallback string. |
| Flowise slash commands (setChatFlow/listChatFlows) | ✅ complete | packages/llm-flowise/src/commands/setChatFlowCommand.ts and listChatFlowsCommand.ts + rest/flowiseSetChatFlow.ts, rest/flowiseListChatFlows.ts — Discord SlashCommandBuilder commands delegating to REST helpers. |
| OpenWebUI direct override client | ✅ complete | packages/llm-openwebui/src/directClient.ts:13-62 — generateChatCompletionDirect accepts apiUrl/model/authHeader overrides, builds proper system+history+user messages. Actually wired into message flow: src/message/handlers/inferenceProcessor.ts:107 (when MESSAGE_LLM_DIRECT) and packages/message-slack |
| OpenWebUI session/password auth | ✅ complete | packages/llm-openwebui/src/sessionManager.ts + openWebUIProvider.ts:16-35 — apiKey auth or password auth via getSessionKey(); test coverage in sessionManager.test.ts. |
| Letta stateful agent chat | ✅ complete | packages/llm-letta/src/lettaProvider.ts:137-198 — client.agents.messages.create for default conversation, conversations.messages.create for session-scoped; extracts assistant text from string/array content. supportsHistory()=false (Letta manages memory). |
| Letta session modes (per-channel/per-user/fixed) | ✅ complete | packages/llm-letta/src/lettaProvider.ts:51-135 — resolveConversationId + getOrCreateConversation lists/creates Letta conversations by summary with per-agent cache keying. Uses (client as any).conversations API (untyped, relies on SDK shape). |
| Letta agent browser (listAgents/getAgent) | ✅ complete | packages/llm-letta/src/agentBrowser.ts:22-81 — real client.agents.list()/retrieve() with isSafeUrl SSRF guard; tolerant response shape parsing. |
| OpenSwarm chat completion | ✅ complete | packages/llm-openswarm/src/OpenSwarmProvider.ts:28-59 — posts OpenAI-compatible /v1/chat/completions, team name from metadata.team/model, bearer auth, 15s timeout; returns content or 'No response'. Errors returned as 'Error: ...' string rather than thrown. |
| OpenSwarm installer (pip install / start daemon) | ✅ complete | packages/llm-openswarm/src/SwarmInstaller.ts — checkPython, checkSwarmInstalled (swarm-cli --version), installSwarm (pip install open-swarm), startSwarm (spawn swarm-api with validated port). Real child_process calls. |
| Default chatbot profile / bot-level provider resolution | ✅ complete | src/llm/getLlmProvider.ts:76-111 (defaultChatbotProfile via UserConfigStore) and getLlmProviderForBot:235-291 (per-bot provider from llmProvider + provider-specific config block, cached). Note: effectiveness limited for openwebui/openswarm/letta due to their create()/singleton config gaps noted abov |
| Flowise non-chat completion | 🟡 partial | packages/llm-flowise/src/flowiseProvider.ts:18,63-66 — supportsCompletion() returns false; generateCompletion() just redirects to chat with channelId:'default-completion'. Functional but not a true completion path. |
| OpenWebUI chat completion (plugin provider) | 🟡 partial | packages/llm-openwebui/src/openWebUIProvider.ts:46-68 posts /chat/completions. BUG/gap: lines 53-55 map ALL history messages to role:'user' (loses assistant turns); model is read once at module load (line 10) from env config and create() (index.ts:9-11) ignores passed config, so bot/profile-level mo |
| OpenWebUI runInference path (knowledge-file RAG) | 🟡 partial | packages/llm-openwebui/src/runInference.ts posts custom payload (prompt/knowledgeFileId/history) to /chat/completions; uploadKnowledgeFile.ts uploads a single startup knowledge file to /v1/files and caches one ID. getKnowledgeFileId() throws if not uploaded (runInference.ts:30 will throw unless a kn |
| OpenWebUI non-chat completion | 🟡 partial | packages/llm-openwebui/src/openWebUIProvider.ts:70-86 posts /completions with max_tokens:100. Real call, but taskLlmRouter's openWebUI wrapper (taskLlmRouter.ts:98-100) throws 'Non-chat completion not supported by OpenWebUI', so behavior depends on path; provider singleton itself supports it. |
| OpenSwarm non-chat completion | 🟡 partial | packages/llm-openswarm/src/OpenSwarmProvider.ts:61-64 — generateCompletion just calls generateChatCompletion with empty history. |
| Per-task LLM routing (semantic/summary/followup/idle) | 🟡 partial | src/llm/taskLlmRouter.ts:206-275 — getTaskLlm resolves env overrides (LLM_TASK_*_PROVIDER/MODEL), per-use-case profiles, and provider instances. But createProviderFromInstance/createProviderFromProfile switch only handles 'openai'|'flowise'|'openwebui' (lines 113-127, 180-195) — letta and openswarm  |
| Live model listing (real API) | 🟡 partial | packages/llm-openai/src/OpenAiService.ts:247-260 + operations/listModels.ts call real openai.models.list(), but OpenAiService.listModels() is never invoked anywhere (grep: no callers). UI fallback ModelAutocomplete.tsx:80-114 does a live direct browser fetch to /models for openai & anthropic only. N |
| Admin model-catalog endpoint /api/admin/llm-providers/:type/models | 🟡 partial | src/server/routes/admin/systemInfo.ts:182-221 (dup in monitoring.ts:395) returns a STATIC hardcoded catalog from src/server/data/llmModels.ts (only openai/anthropic/google/perplexity). Not a live query and excludes the actually-implemented providers flowise/letta/openwebui/openswarm. ModelAutocomple |
| Letta non-chat completion | 🔲 stub | packages/llm-letta/src/lettaProvider.ts:200-202 — generateCompletion throws 'Letta provider does not support non-chat completion.' (intentional). |
| Vision (image input) support | 🔲 stub | Only static metadata flags supportsVision in src/server/data/llmModels.ts:41 etc. No provider constructs image content parts or passes images to any chat API — all message mapping uses plain text (e.g. openAiProvider.ts:121-126 content: getText()). UI-displayed capability is faked by the static cata |
| Function/tool calling (provider-native) | 🔲 stub | Static supportsFunctionCalling flags in src/server/data/llmModels.ts only. No provider sends tools/functions to its API (OpenAI call openAiProvider.ts:151-159 passes only model/messages/max_tokens/temperature). Tool augmentation is handled out-of-band via toolAugmentedCompletion (inferenceProcessor. |
| Streaming chat completion (generateStreamingChatCompletion) | 📋 planned | Declared optional in src/llm/interfaces/ILlmProvider.ts:109-115 and packages/shared-types/src/ILlmProvider.ts, but NO provider implements it (grep finds only interface + docs). Flowise SDK explicitly sets streaming:false (flowiseSdkClient.ts:28). BotTestDriveTab.tsx UI shows 'streamingContent' but n |
| Letta per-bot config isolation | ❌ broken | packages/llm-letta/src/lettaProvider.ts:30-35 + index.ts:9-11 — create() returns LettaProvider.getInstance(config); singleton only honors the FIRST config. Auth uses process.env.LETTA_SERVER_PASSWORD only (line 26). A second bot/profile with different agentId/config silently reuses the first instanc |
| OpenSwarm config wiring | ❌ broken | packages/llm-openswarm/src/OpenSwarmProvider.ts:15-18 constructor takes NO args and reads only OPENSWARM_BASE_URL/OPENSWARM_API_KEY env vars; index.ts:9-11 create(_config) ignores config. So bot/profile-level OpenSwarm config (baseUrl/apiKey) cannot be applied — only env vars work. |

## memory

| Feature | Status | Evidence |
|---|---|---|
| IMemoryProvider canonical contract | ✅ complete | packages/shared-types/src/IMemoryProvider.ts:62-134 defines the full superset interface (addMemory, searchMemories, getMemories, getMemory, updateMemory, deleteMemory, deleteAll, healthCheck) plus MemoryEntry/MemorySearchResult/MemoryScopeOptions types. mem0 and mem4ai implement it fully; postgres i |
| Mem0 backend (store) | ✅ complete | packages/memory-mem0/src/Mem0Provider.ts:75-91 addMemory POSTs to /memories/ with messages/user_id/agent_id/metadata via real fetch transport (doRequest, lines 283-365) with retries, timeout, circuit breaker, and SSRF isSafeUrl guard. Token auth header line 291. |
| Mem0 backend (recall/search) | ✅ complete | Mem0Provider.ts:93-110 searchMemories POSTs /memories/search/ and applies a client-side threshold filter (line 108). getMemories (112-122), getMemory (124-137 with 404->null) all real. |
| Mem0 backend (update) | ✅ complete | Mem0Provider.ts:139-159 updateMemory PUTs /memories/{id}/ with text+metadata; returns synthesized entry with timestamp Date.now(). |
| Mem0 backend (delete / deleteAll / health) | ✅ complete | Mem0Provider.ts:161-188: deleteMemory DELETE /memories/{id}/, deleteAll DELETE with user/agent scope query params, healthCheck does a GET limit=1 probe and maps to ok/error. |
| Mem0 legacy convenience API (add/search/getAll/get/update/delete) | ✅ complete | Mem0Provider.ts:194-271 provides a second messages-array style API for older callers, fully wired to the same transport. |
| Mem4ai backend (store/recall/list/get) | ✅ complete | packages/memory-mem4ai/src/Mem4aiProvider.ts:80-146; nearly identical real implementation to Mem0 but Bearer auth (line 307), X-Organization-ID header, embedding_provider_id passthrough, and configurable default limit (line 109). Requires both apiKey and apiUrl (constructor 46-51). |
| Mem4ai backend (update/delete/deleteAll/health) | ✅ complete | Mem4aiProvider.ts:148-284 implements updateMemory, deleteMemory, deleteAll, healthCheck plus the legacy add/search/getAll/get/update/delete API; transport in doRequest 292-381 with retry/backoff/circuit breaker. |
| Postgres/pgvector backend (store) | ✅ complete | packages/memory-postgres/src/PostgresMemoryProvider.ts:63-89 addMemory generates an embedding via the resolved LLM embedding provider then calls dbManager.addMemory; backed by MemoryRepository.addMemory (src/database/repositories/MemoryRepository.ts:13-47) which INSERTs into the memories table with  |
| Postgres/pgvector backend (vector similarity recall) | ✅ complete | PostgresMemoryProvider.ts:91-117 embeds the query and calls dbManager.searchMemories; MemoryRepository.searchMemories (49-136) does real cosine distance ordering (embedding <=> ?::vector, score = 1 - distance) on Postgres AND a JS-side cosine-similarity fallback for SQLite (87-131). Threshold filter |
| Postgres backend (list/get/delete/deleteAll/health) | ✅ complete | PostgresMemoryProvider.ts:119-187 getMemories, getMemory, deleteMemory, deleteAll, healthCheck all delegate to DatabaseManager (src/database/DatabaseManager.ts:540-563) and MemoryRepository; healthCheck checks dbManager.isConnected(). |
| Memory profiles config (CRUD/validation) | ✅ complete | src/config/memoryProfiles.ts:38-172 Zod-validated memory-profiles.json with per-provider config bag (passthrough for forward-compat), loadMemoryProfiles/saveMemoryProfiles/getMemoryProfileByKey. Maps profile.provider -> memory-<provider> plugin. |
| SyncProviderRegistry memory provider loading (with deps) | ✅ complete | src/registries/SyncProviderRegistry.ts:148-161 loads memory-<provider> plugins and crucially passes getServiceDependencies(...) into instantiateMemoryProvider (line 151-152), so Postgres gets its DatabaseManager + embedding providers here. Exposes getMemoryProvider/getMemoryProviders (329-336). |
| REST API: list memory providers + health | ✅ complete | src/server/routes/providers.ts:58-103 GET /api/providers/memory iterates registry.getMemoryProviders() and reports per-provider healthCheck status. Real registry data, not mock. |
| SQLite/Postgres memories schema + indexes | ✅ complete | src/database/migrations/000_initial_schema.ts:293-305 creates memories table (embedding vector(1536) on PG, TEXT on SQLite) with userId/agentId/sessionId/created_at; indexes on userId, agentId, HNSW vector_cosine_ops index (368-377); migration 001 adds created_at DESC index. CREATE EXTENSION vector  |
| Circuit breaker resilience for remote memory APIs | ✅ complete | mem0 uses shared @common/CircuitBreaker (Mem0Provider.ts:10-12,56-61); mem4ai ships its own packages/memory-mem4ai/src/CircuitBreaker.ts (with tests). Both wrap every request (request()->circuitBreaker.execute) with configurable failureThreshold/resetTimeout/halfOpen settings, plus exponential backo |
| Postgres getMemory by id (inefficient impl) | 🟡 partial | PostgresMemoryProvider.ts:137-153 has no real single-row query: it loads up to 1000 memories via getMemories({limit:1000}) and Array.find()s for the id (comment line 139-140 'For now we use getMemories and find'). Works for small datasets but will miss memories beyond the 1000 cap and is O(n). |
| MemoryManager pipeline wiring (store + retrieve conversation memory) | 🟡 partial | src/services/MemoryManager.ts resolves a bot's memoryProfile to a provider, caches it, storeConversationMemory (141-173) and retrieveRelevantMemories (185-215) wrap provider calls in withTimeout(5s) and swallow errors so memory never breaks the pipeline. Wired via MemoryStorerAdapter/MemoryRetriever |
| REST API: memory provider smoke test (add/search/get/delete) | 🟡 partial | src/server/routes/providers.ts:112-272 POST /api/providers/memory/:name/test runs a real round-trip: addMemory -> searchMemories('What is my favourite colour?') -> getMemory -> deleteMemory against the live provider (lines 163,187,211,239). Does NOT cover updateMemory or deleteAll, so the unimplemen |
| Package build/distribution wiring | 🟡 partial | All three real memory packages declare main: dist/index.js (memory-mem0/mem4ai/postgres package.json) but per CLAUDE.md dist/ is not maintained; tsx resolves packages via src/ at runtime, so this works in dev but the stated build artifacts don't exist. mem0/mem4ai package.json test scripts are stubs |
| Postgres backend (update) | 🔲 stub | PostgresMemoryProvider.ts:155-161 updateMemory unconditionally throws 'Update memory not implemented in PostgresMemoryProvider'. MemoryRepository has no update method either. Provider test route never exercises update so the gap is silent. |
| MemVault backend (pgvector hybrid scoring) | 📋 planned | packages/memory-memvault/ contains ONLY a stray node_modules dir (with pg/debug/typescript) — no package.json, no src/, no README. docs/reference/IMPROVEMENT_ROADMAP.md:98,103 claims '@hivemind/memory-memvault ... implemented' with 'hybrid scoring (vector × 0.8) + (recency decay × 0.2)', but no code |
| Memory summarization | 📋 planned | No memory-summarization feature exists. The only 'summary' artifacts are a conversation_summaries table (migrations/000_initial_schema.ts:39) with no producer in the memory domain, and unrelated llm/tasks/summarizeText.ts + LogProseSummarizer.ts (log/text summarizers, not memory). No provider or Mem |
| Memory eviction / TTL / retention / pruning | 📋 planned | Grep for evict/expire/ttl/prune/retention/maxMemories across the codebase returns only unrelated cache TTLs (src/utils/TTLCache.ts, apiCache.ts) and DB maintenance/backup services. No memory backend, MemoryRepository, or MemoryManager implements eviction, size caps, age-based expiry, or recency-deca |

## mcp-tools

| Feature | Status | Evidence |
|---|---|---|
| MCPGuard (owner/custom access control) | ✅ complete | src/mcp/MCPGuard.ts:34-85. isUserAllowed handles disabled/owner/custom/default-deny correctly; validateConfig validates type and custom allowedUserIds. Pure logic, fully implemented and unit-testable. Wired into MCPService.assertGuardAllowsExecution and ToolManager. |
| HITL sensitive-tool approval (PendingActionManager) | ✅ complete | src/services/ToolManager.ts:172-185 gates sensitiveTools; src/managers/PendingActionManager.ts has real promise-based create/requestApproval (line 84-92) with pending action store and getPendingActions (135). Approval mechanism is real (resolves a Promise<boolean>). |
| MCP provider templates | ✅ complete | src/config/mcp/toolRegistry.ts:133-260 returns 5 concrete templates (filesystem/web-scraper/github/postgres/slack) with createFromTemplate. Exposed via GET /api/mcp/providers/templates (providers.ts:60-72). Real static data wired end-to-end. |
| MCP provider config validation | ✅ complete | src/config/mcp/configLoader.ts:10-125 validates name/command/type/timeout, blocks dangerous shell commands (sh/bash/rm/etc, 33-52), enforces command-format allowlist, emits warnings/suggestions. Used on every add/update. |
| SSRF protection on MCP server URLs | ✅ complete | src/server/routes/admin/mcpServers.ts:58,120,375 call isSafeUrl (src/utils/ssrfGuard) before connect/test/restart and return 403 on private/local targets. Real, applied on the admin path. (Note: blocks localhost, which conflicts with templates.ts localhost defaults.) |
| MCP connection persistence (webUIStorage / mcp-servers.json) | ✅ complete | Admin path: webUIStorage.saveMcp/getMcps/deleteMcp (mcpServers.ts:137,199,223). mcp/servers path: loadMCPServers/saveMCPServers to data/mcp-servers.json (shared.ts:47-70). Both persist configs; admin path sanitizes API key to first-3-chars+*** before storage (mcpServers.ts:131). |
| Bot tool-calling pipeline (ToolManager + toolAugmentedCompletion) | 🟡 partial | src/services/ToolManager.ts aggregates tools per bot (getToolsForBot 68-119), formats for OpenAI (219-228), enforces sensitiveTools HITL approval (172-185), routes execution with timeout (189-200). Wired into the live pipeline via src/message/handlers/inferenceProcessor.ts:116 -> src/services/toolAu |
| MCPProviderManager — child-process MCP provider lifecycle | 🟡 partial | src/config/mcp/index.ts + serverLifecycle.ts implement add/update/remove/start/stop/restart, status tracking, health checks, stats, import/export via DI (tsyringe). startProvider spawns a real child process (serverLifecycle.ts:175-215). Functional CRUD/lifecycle, BUT contains a bug (see broken entry |
| /api/mcp/providers CRUD + lifecycle + test routes | 🟡 partial | src/server/routes/mcp/providers.ts:18-420 fully wires GET list/templates/stats/:id, POST create, PUT update, DELETE, start/stop/test. Mounted at /api/mcp via registerRoutes.ts:144 (authenticateToken) and again under admin index.ts:65. CRUD is complete; start/test depend on MCPProviderManager which h |
| /api/mcp/servers CRUD + connect/call-tool (shared.ts stdio clients) | 🟡 partial | src/server/routes/mcp/servers.ts uses src/server/routes/mcp/shared.ts which correctly imports the SDK via subpaths (@modelcontextprotocol/sdk/client/index.js & /client/stdio.js) and a real StdioClientTransport. connect/listTools/callTool/disconnect work. LIMITATION: connectToMCPServer (shared.ts:73- |
| MCP tool listing/testing routes (/api/admin/mcp-tools) | 🟡 partial | src/server/routes/mcpToolsTesting.ts:16-153 (GET /list, POST /test) reads connectedClients from shared.ts and calls client.callTool directly — real, and depends on the working shared.ts stdio path. Functional only for stdio servers connected via /api/mcp/servers; returns empty when nothing connected |
| MCPServersPage (client) — server list/CRUD UI | 🟡 partial | src/client/src/pages/MCPServersPage/hooks/useMCPServerData.ts:53 correctly reads data.data.servers/configurations/trusted repos from GET /api/admin/mcp-servers (list works). useMCPServerActions.ts connect/test/save POST to /api/admin/mcp-servers/connect|test — these backend handlers are broken (MCPS |
| MCPToolsPage tool registry/execution/history (client) | 🟡 partial | src/client/src/pages/MCPToolsPage/hooks/useToolRegistry.ts loads tools from /api/mcp/servers (real) and reads json.servers||json.data?.servers (handles both shapes). Tool execution (useToolExecution.ts:29) posts to real /api/mcp/servers/:name/call-tool. HOWEVER preferences load (useToolRegistry.ts:5 |
| MCP server templates (src/mcp/templates.ts) | 🟡 partial | src/mcp/templates.ts:21-76 defines MCP_TEMPLATES (google-search, sqlite, filesystem, fetch, mem0) with http://localhost default URLs. Static data only; not referenced by any route or page found in this domain (separate from the toolRegistry templates served via /api/mcp/providers/templates). Effecti |
| Three unintegrated MCP subsystems (architectural gap) | 🟡 partial | Three independent connection stores never share state: (1) MCPService.clients (src/mcp/MCPService.ts) consumed by ToolManager for bots; (2) MCPProviderManager processes (src/config/mcp/serverLifecycle.ts) consumed by /api/mcp/providers; (3) connectedClients Map (src/server/routes/mcp/shared.ts) cons |
| Built-in transfer_to_bot swarm-routing tool | 🔲 stub | src/services/ToolManager.ts:145-157. Returns a canned success string telling the LLM to stop; comment at line 150-151 explicitly says 'In a full implementation, we'd reconstruct the IMessage and emit message:incoming. For now, we return a success signal'. No actual handoff/message re-emission occurs |
| /api/mcp-tools routes (mcpTools.ts) | 🔲 stub | src/server/routes/mcpTools.ts:15-25 — GET /list returns hardcoded {tools:[],count:0}; GET /test returns {status:'ok'}. Pure placeholder. Mounted at /api/mcp-tools (registerRoutes.ts:145). |
| MCPService remote-server connection (SDK Client) | ❌ broken | src/mcp/MCPService.ts:57,150 use require('@modelcontextprotocol/sdk') and destructure {Client}. The installed SDK has NO root CJS entry (package.json exports['.'].require -> ./dist/cjs/index.js which does not exist; verified `require('@modelcontextprotocol/sdk')` throws 'Cannot find module .../dist/ |
| MCPService.executeTool + guard enforcement | ❌ broken | src/mcp/MCPService.ts:309-387. assertGuardAllowsExecution (389-432) is sound (requires botName, resolves owner via Slack/Discord providers, calls MCPGuard.isUserAllowed). BUT executeTool requires this.clients.get(serverName) (329-332), and clients are only populated by connectToServer which is broke |
| MCPProviderManager start: spawn error handling | ❌ broken | src/config/mcp/serverLifecycle.ts:194 registers providerProcess.on('configuration', ...) — ChildProcess emits no 'configuration' event; the correct event is 'error'. Spawn failures (e.g. ENOENT, command not found) are never caught/rejected and instead fall through to the timeout path (208-214), prod |
| MCP provider test (executeProviderTest) | ❌ broken | src/config/mcp/toolRegistry.ts:11-92 spawns the command and inspects stdout for version/capabilities via regex (extractVersion/extractCapabilities 94-131) — a heuristic, not a real MCP handshake. Also line 74 mcpProcess.on('configuration', ...) is the wrong event name (should be 'error'), so spawn e |
| /api/admin/mcp-servers management routes (MCPService-backed) | ❌ broken | src/server/routes/admin/mcpServers.ts:31-471 implements test/connect/disconnect/delete/list/status/restart/bulk-disconnect with SSRF guard (isSafeUrl) and rate limiting — well structured. BUT all connect/test/restart paths call MCPService.connectToServer/testConnection which are broken (root SDK req |
| MCPToolsTestingPage (client) | ❌ broken | src/client/src/pages/MCPToolsTestingPage.tsx:53 reads json.servers but GET /api/mcp/servers returns {data:{servers}} (servers.ts:36) — so serverList is always [] and no tools ever populate. Also line 199 renders <Textarea> which is NOT imported (line 16 imports Input/Select/Button only) -> Reference |
| MCPServerManager component (legacy) | ❌ broken | src/client/src/components/MCPServerManager.tsx:55 does `if (data.servers) Object.entries(data.servers)` but /api/admin/mcp-servers returns an ARRAY at data.data.servers, not an object at data.servers. So the table always renders empty. Appears superseded by the MCPServersPage/ directory which parses |
| McpToolProvider (packages/tool-mcp) | ❌ broken | packages/tool-mcp/src/McpToolProvider.ts:133 uses require('@modelcontextprotocol/sdk') and destructures {Client} — same broken root-require as MCPService (no CJS root entry). client.connect({url,apiKey}) (144-147) also mismatches the real SDK transport API. Implements IToolProvider with circuit brea |

## bots-config

| Feature | Status | Evidence |
|---|---|---|
| Bot list/get with live status & stats | ✅ complete | src/server/routes/bots.ts:58-199 lists/gets bots via BotManager.getAllBots/getBot, merges running status from getBotsStatus and message/error counts from WebSocketService.getBotStats. Wired to client src/client/src/pages/BotsPage/hooks/useBotsList.ts:22 GET /api/bots. |
| Bot create (catalog CRUD) | ✅ complete | src/server/routes/bots.ts:208-223 -> BotManager.createBot (src/managers/BotManager.ts:198-243): validates, generates UUID, stores secure config via SecureConfigManager, persists to webUIStorage, emits botCreated. Idempotent on duplicate name. |
| Bot update | ✅ complete | src/server/routes/bots.ts:232-249 -> BotManager.updateBot (BotManager.ts:307-358): merges config/persona/systemInstruction/mcp, re-stores secure config, persists. Note: this catalog-update path differs from the approval-gated /api/bot-config PUT path. |
| Bot delete | ✅ complete | src/server/routes/bots.ts:258-272 -> BotManager.deleteBot (BotManager.ts:363+), idempotent when bot already absent. |
| Bot clone | ✅ complete | src/server/routes/bots.ts:281-298 -> BotManager.cloneBot (BotManager.ts:248-302) handles both custom (webUIStorage) and file-configured bots; idempotent on duplicate newName. |
| Bot start/stop/restart lifecycle | ✅ complete | src/server/routes/bots.ts:307-350 -> BotManager.startBot/stopBot; restart used by version restore at bots.ts:498. Backed by src/managers/botLifecycle.ts. |
| Bot reorder | ✅ complete | src/server/routes/bots.ts:97-105 -> BotRouteService.reorderBots (src/server/services/BotRouteService.ts:28-33) writes config/user/bot-order.json. |
| Bot export (single & all, secrets redacted) | ✅ complete | bots.ts:114-128 & 511-531 -> BotRouteService.sanitizeBotForExport (BotRouteService.ts:175-198) redacts token/apiKey/secret keys, drops envOverrides, adds schemaVersion. |
| Bot import (create/update report) | ✅ complete | bots.ts:137-152 -> BotRouteService.importBots (BotRouteService.ts:35-69) upserts by case-insensitive name, returns created/updated/errors report. |
| AI bot config generation | ✅ complete | bots.ts:540-554 -> BotRouteService.generateConfig (BotRouteService.ts:71-96) calls real LLM provider with a designer system prompt and parses JSON. Depends on an available LLM provider (throws if none). |
| Bot deep diagnostic | ✅ complete | bots.ts:563-578 -> BotRouteService.runDiagnostic (BotRouteService.ts:98-160) actually probes the messenger service connection, pings the LLM provider for latency, and checks MCP connected servers. |
| Test-chat sandbox (persona dry-run) | ✅ complete | bots.ts:587-601 -> BotRouteService.testChat (BotRouteService.ts:162-173) runs a real LLM chat completion with the supplied systemInstruction/history. |
| AI performance insights | ✅ complete | bots.ts:610-625 -> BotInsightsService.generateInsights (src/server/services/BotInsightsService.ts:32-87) pulls BotMetricsService + AnomalyDetectionService data and asks the LLM for a markdown review. Real implementation (throws if no LLM). |
| Bot benchmark | ✅ complete | bots.ts:634-649 -> BotBenchmarkService.runBenchmark (src/server/services/BotBenchmarkService.ts:44-96) runs 4 fixed prompts through the real provider measuring latency/length. Caveat: iqScore is hardcoded to 85 (BotBenchmarkService.ts:92), so the 'IQ' metric is not actually computed. |
| Adversarial stress test (jailbreak eval) | ✅ complete | bots.ts:658-673 -> BotStressTestService.runStressTest (src/server/services/BotStressTestService.ts:51-189) drives the real InferenceStage over ADVERSARIAL_PROMPTS and uses an LLM safety-evaluator to classify pass/fail. Full implementation. |
| Bot config version history & restore | ✅ complete | bots.ts:443-502 -> ConfigurationVersionService.getVersionHistory/restoreVersion against DatabaseManager.getBotConfigurationByName; restore also restarts the bot. Returns empty list gracefully when no DB config exists. |
| Bot activity log (PII-redacted) | ✅ complete | bots.ts:391-434 -> ActivityLogger.getEvents filtered by bot name; channelId/userId redacted before return. |
| Bot chat history | ✅ complete | bots.ts:359-382 -> BotManager.getBotHistory (BotManager.ts:570) with limit clamped 1-100. |
| Persona bulk delete with bot reversion | ✅ complete | personas.ts:213-297 awaits deletePersona, blocks built-in deletion, finds bots referencing the persona and reverts them to 'default' before deleting; returns detailed report. |
| Persona reorder | ✅ complete | personas.ts:85-110 writes config/user/persona-order.json (order is persisted; note getAllPersonas does not appear to re-apply this ordering on read). |
| Guard profiles CRUD (MCP guard, rate limit, content filter) | ✅ complete | src/server/routes/guardProfiles.ts:33-253 full CRUD with prototype-pollution-safe merge, mounted at /api/admin/guard-profiles (src/server/routes/admin/index.ts:67). Persisted via src/config/guardrailProfiles.ts loadGuardrailProfiles/saveGuardrailProfiles to guardrail-profiles.json with 4 default pro |
| Guard profiles bulk delete/toggle | ✅ complete | guardProfiles.ts:256-335 atomic bulk delete and bulk toggle (sets enabled on mcpGuard/rateLimit/contentFilter), saved atomically. |
| Guard profile test (synthetic eval) | ✅ complete | guardProfiles.ts:338-468 evaluates access-control/rate-limit/content-filter against testInput and returns per-guard allowed/blocked with overall result. This is a self-contained simulation (not the real runtime guard pipeline), but it is a real, working feature for the UI's 'test' button. |
| Access-control guard (users/IPs) + per-guard toggle | ✅ complete | src/server/routes/guards.ts:17-118 GET/POST/:id/toggle backed by webUIStorage.getGuards/saveGuard/toggleGuard (src/storage/webUIStorage.ts:414-512, seeds default access-control/rate-limiting guards). IP octet validation enforced. Mounted /api/guards. |
| Onboarding wizard (status/step/complete/reset, persisted) | ✅ complete | src/server/routes/onboarding.ts persists state to data/onboarding.json, auto-detects completion only when bots exist AND an LLM provider is configured (onboarding.ts:68-74). Client OnboardingPage.tsx:518-617 fetches personas/profiles, syncs step, creates bot + saves global LLM config, then marks com |
| Bot configuration via approval workflow (/api/bot-config) | ✅ complete | src/server/routes/botConfig.ts:273-395 PUT validates (convict schema + business rules) then creates a DB approval request rather than applying directly; botConfig.ts:397-570 apply-update applies only after approval, updating UserConfigStore overrides + SecureConfigManager and reloading. Backed by Bo |
| Bot-config create (admin, DB-backed) | ✅ complete | botConfig.ts:216-267 -> BotConfigService.createBotConfig (BotConfigService.ts:115-169) validates, checks name uniqueness, serializes provider blocks, writes to DB and audit log. Requires DB configured (returns 503 otherwise). |
| Global defaults config editor (convict) + rollbacks | ✅ complete | src/client/src/pages/BotConfigurationPage.tsx is the 'Global Defaults' editor over /api/config/global with snapshot rollback modal (BotConfigurationPage.tsx:337-418). Note: despite the file name this edits global convict configs, not per-bot config. |
| Config import/export service (backup/encrypt/CSV/YAML) | ✅ complete | src/server/services/ConfigurationImportExportService.ts:38-60 real DB-backed service with BackupManager, checksum, compress/encrypt, CSV/YAML conversion and version service integration. |
| BotCreatePage UI | ✅ complete | src/client/src/pages/BotCreatePage.tsx provides name/description/systemInstruction inputs feeding POST /api/bots; integrates with the AI generate-config flow. |
| Scheduled bot tasks (recurring prompts) | 🟡 partial | bots.ts:682-705 -> BotTaskScheduler.scheduleTask (src/server/services/BotTaskScheduler.ts). Tasks held only in an in-memory array; loadTasks() (line 75-78) is an empty stub ('mocked for now') with no DB load, and scheduleTask never persists, so all scheduled tasks are lost on restart. No DELETE/list |
| Persona system (list/get/create/clone/update/delete, file-persisted) | 🟡 partial | src/managers/PersonaManager.ts is a real file-backed store (config/user/custom-personas.json, seeds editable copies of BUILTIN_PERSONAS, src/managers/PersonaManager.ts:147-333). Manager itself is complete, but the create/clone/update routes have a bug (see next item) that undermines correct use. |
| Legacy/parallel persona store (/api/agents/personas) | 🟡 partial | src/server/routes/agents.ts:213-289 implements a SECOND, separate persona CRUD backed by data/personas.json with a different shape (key/name/systemPrompt). Duplicates the PersonaManager system and can diverge from /api/personas; the two stores are not reconciled. |
| Semantic guardrails (LLM input/output) profile + test | 🟡 partial | guardrailProfiles.ts defines semanticInputGuard/semanticOutputGuard in the default 'semantic-protected' profile (src/config/guardrailProfiles.ts:53-148) and src/server/routes/guards.ts:121-181 POST /semantic/test wires to SemanticGuardrailService.evaluateInput/Output. However the guardProfiles CRUD  |
| Bot-config templates | 🟡 partial | botConfig.ts:96-163 returns 3 hardcoded templates (discord_openai, slack_flowise, mattermost_openwebui) inline in the route — static data, not a managed/extensible template system. There is a richer ConfigurationTemplateService but this endpoint does not use it. |
| GuardsPage 'Guard Settings' / advanced rules tab | 🔲 stub | src/client/src/pages/GuardsPage.tsx:155-248 — tab labeled with 'placeholder — no settings API yet' and renders 'Guard settings coming soon ... connected in a future release' and 'Advanced guard settings coming soon'; the JSON rules textarea is non-functional. |
| Persona create / clone / update HTTP responses | ❌ broken | src/server/routes/personas.ts:152 (POST /), :178 (POST /:id/clone), :202 (PUT /:id) call manager.createPersona/clonePersona/updatePersona WITHOUT await, but those methods are async (PersonaManager.ts:245,279,261 return Promises). The handler serializes a pending Promise -> responds with empty/incorr |

## pipeline

| Feature | Status | Evidence |
|---|---|---|
| 5-stage MessageBus pipeline orchestration (createPipeline) | ✅ complete | src/pipeline/createPipeline.ts:67-99 wires ReceiveStage->DecisionStage->EnrichStage->InferenceStage->SendStage on a MessageBus via adapter-backed deps and registers a PipelineTracer. Each stage subscribes to the prior event and emits the next (e.g. EnrichStage.ts:71, InferenceStage.ts:75, SendStage. |
| Stage 1 ReceiveStage (sanitize + validate + panic-mode gate) | ✅ complete | src/pipeline/ReceiveStage.ts:53-110: strips control chars (sanitize, line 127), rejects empty text, consults PanicModeService kill switch (lines 59-69), builds MessageContext, persists sanitized text via message.setText, emits message:validated. |
| Stage 2 DecisionStage (should-reply + swarm claim) | ✅ complete | src/pipeline/DecisionStage.ts:84-199: checks SwarmCoordinator existing claims (107-136), runs injected DecisionStrategy (139), claims message on accept (151), broadcasts pipeline:decision to WebSocket, emits message:accepted/skipped/error, and honors PipelineDebuggerService breakpoints (87-98). |
| Stage 3 EnrichStage (memory retrieval + system prompt) | ✅ complete | src/pipeline/EnrichStage.ts:80-119: retrieves memories with a 5s timeout race (retrieveWithTimeout, 129-158), graceful degradation to empty memories, builds system prompt via PromptBuilder, emits message:enriched. Adapters MemoryRetrieverAdapter.ts and PromptBuilderAdapter.ts are real (delegate to M |
| Stage 4 InferenceStage (LLM call + token budget + logging) | ✅ complete | src/pipeline/InferenceStage.ts:87-191: token-budget pre-check via TokenBudgetService (99-112) with admin alert + bot stop on overage, calls LlmInvoker, increments usage heuristically (~4 chars/token, 149), logs to DatabaseManager.logInference on success/empty/error, emits message:response/skipped/er |
| Stage 5 SendStage (split + send + memory store) | ✅ complete | src/pipeline/SendStage.ts:83-129: trims, splits into <=2000-char parts respecting newline/space boundaries (splitMessage, 137-171), sends each part via MessageSenderAdapter (->IMessengerService.sendMessageToChannel), emits message:sent, and fire-and-forget stores user+assistant memory via MemoryStor |
| Legacy handler fallback (USE_LEGACY_HANDLER) | ✅ complete | src/server/initServices.ts:93-126: when USE_LEGACY_HANDLER==='true' the message handler calls handleMessage() directly (messageHandler.ts:60); otherwise it emits message:incoming onto the bus. Default is pipeline. startupDiagnostics.ts:147,492 documents the flag. Legacy path (messageHandler.ts:82-16 |
| Response decisioning / probabilistic scoring (shouldReplyToMessage) | ✅ complete | src/message/helpers/processing/shouldReplyToMessage.ts:130-670: full additive/multiplicative scoring — base chance, recent-activity bonus, participant/bot-history/user-count/token-density penalties, bot-ratio, on-topic, addressed-to-other, leading-address, mention/reply/question modifiers, burst-tra |
| IncomingMessageDensity tracking | ✅ complete | src/message/helpers/processing/IncomingMessageDensity.ts: singleton tracks per-channel message history (5-min window) and per-participant last-seen; getDensity/getUniqueParticipantCount/getDensityModifier all implemented and consumed by shouldReplyToMessage.ts:287,414,563. recordMessageAndGetModifie |
| Semantic relevance / on-topic boost (isOnTopic) | ✅ complete | src/message/helpers/processing/SemanticRelevanceChecker.ts:64-119: gated by MESSAGE_SEMANTIC_RELEVANCE_ENABLED, runs a cheap 5-token LLM classification via getTaskLlm('semantic'), fails open/closed appropriately; consumed in shouldReplyToMessage.ts:456. isNonsense (142-182) also implemented but not  |
| Idle / inactivity response generation | ✅ complete | src/message/management/IdleResponseManager.ts: schedules randomized idle prompts (min/max delay env-configurable), generates an LLM follow-up referencing the last non-self message via getTaskLlm('idle') (485-511) with static-prompt fallback, enforces at-most-once per interaction window, records bot  |
| Pipeline observability (PipelineTracer / stats) | ✅ complete | src/observability/PipelineTracer.ts builds per-message timing spans into a ring buffer and exposes getStats (PipelineStats: totalTraces/avgDurationMs/stageAvgMs/errorRate). Registered and stored via setActiveTracer in createPipeline.ts:96-98; integrates AnomalyDetectionService. TraceExporter.ts prov |
| Swarm coordination / message claiming | ✅ complete | DecisionStage.ts:102-164 uses SwarmCoordinator.getInstance().getClaim()/claimMessage() to prevent multiple bots replying to the same message; skip-if-claimed path emits message:skipped with claimedBy metadata and broadcasts pipeline:decision. |
| Message splitting for long responses | ✅ complete | src/pipeline/SendStage.ts:137-171 splits at MAX_PART_LENGTH=2000 preferring newline then space boundaries, hard-splits only as last resort, filters empty parts. Mirrors legacy splitMessageContent helper. |
| Feature parity between pipeline and legacy handler | 🟡 partial | The default pipeline omits major legacy stages: content filtering (ContentFilterService), semantic input guardrail (SemanticGuardrailService), command handling (handleCommands), duplicate detection, maintenance-mode check, audit logging, and channel delay/typing scheduling — all present in messageHa |
| Unsolicited message gating | 🟡 partial | src/message/helpers/unsolicitedMessageHandler.ts:19-62: shouldReplyToUnsolicitedMessage now unconditionally returns true (all hard blocks removed per the refactor comment, lines 20-22); decisions are deferred to probability modifiers. looksLikeOpportunity (64-88) is fully implemented but unused. The |
| Pipeline debugger breakpoints | 🟡 partial | src/server/services/PipelineDebuggerService.ts implements toggleBreakpoint/shouldPause/pause/resume with EventEmitter, but DecisionStage.ts:87-98 is the only stage that actually checks shouldPause('validated'). Other stages (Enrich/Inference/Send) never call the debugger, so breakpoints only work at |
| Pipeline LLM provider resolution uses real per-bot config | ❌ broken | src/server/initServices.ts:461-466 calls createPipeline with botConfig: {} (empty). LlmInvokerAdapter (src/pipeline/adapters/LlmInvokerAdapter.ts:38) resolves the provider from this.deps.botConfig (the empty {}), NOT the per-message ctx.botConfig. getLlmProviderForBot (src/llm/getLlmProvider.ts:235- |
| Per-service pipeline registration on shared singleton bus (fan-out duplication) | ❌ broken | src/server/initServices.ts:460-467 loops over messengerServices calling createPipeline(bus, ...) once per service, but MessageBus.getInstance() (src/events/MessageBus.ts:58) is a singleton and stages register additive listeners (on(), addListener 234-247). With N services, each message:incoming even |
| Global activity / fatigue penalty | ❌ broken | GlobalActivityTracker.getScore is read in shouldReplyToMessage.ts:575 to apply a fatigue penalty, but GlobalActivityTracker.recordActivity (src/message/helpers/processing/GlobalActivityTracker.ts:43) is never called anywhere in production code (grep recordActivity finds only its definition). The sco |
| Grace-window / recently-posted bonus in pipeline mode | ❌ broken | shouldReplyToMessage relies on getLastBotActivity (ChannelActivity.ts:27) for the grace window (lines 247-250) and hasPostedRecently bonus (380,400). recordBotActivity is only called from legacy inputProcessor.ts:296,307 and outputProcessor.ts:161 and IdleResponseManager.ts:526 — NOT from the pipeli |
| Idle-response interaction recording in pipeline mode | ❌ broken | IdleResponseManager.recordInteraction (which actually schedules idle timers) is only called from src/message/handlers/replyDecision.ts:15 — part of the LEGACY handleMessage path. The default pipeline (src/pipeline/**) never calls recordInteraction (grep confirms no occurrence in pipeline), so idle r |

## monitoring

| Feature | Status | Evidence |
|---|---|---|
| Real-time performance metrics (CPU/mem/connections/responseTime) | ✅ complete | src/server/services/websocket/broadcast/MetricCalculator.ts:32-77 computes genuine CPU% via process.cpuUsage()+hrtime delta, memory via os.totalmem/freemem, active connections from socket count; broadcast over WS (BroadcastService.ts:304). Not mocked. |
| System health page / HealthCheckWidget (AdminHealthPage) | ✅ complete | src/client/src/pages/AdminHealthPage.tsx -> SystemHealth.tsx + HealthCheckWidget.tsx fetch apiService.getSystemHealth()='/health/detailed' and getServiceHealth()='/health/detailed/services'. src/server/routes/health/detailed.ts:133+ performs real DB/LLM/messenger/memory-provider checks with latency  |
| Basic & detailed /health endpoints | ✅ complete | src/server/routes/health/basic.ts checks real DatabaseManager.isConnected() and memory providers via ProviderRegistry.healthCheck(); detailed.ts returns real process/os info and per-service status. |
| Prometheus metrics endpoint (/health/metrics/prometheus) | ✅ complete | src/server/routes/health/metrics.ts:39-81 emits real process uptime/memory/CPU plus MetricsCollector.getInstance().getPrometheusFormat(); ProviderMetricsCollector.ts uses prom-client registry. Counters real where fed. |
| AI analytics: behavior patterns / user segments / recommendations (/api/dashboard/ai/*) | ✅ complete | src/server/routes/dashboard.ts:49-181 -> AnalyticsService (src/services/AnalyticsService.ts) -> real PatternAnalyzer/SegmentClassifier/RecommendationEngine/TimeSeriesAggregator computing over ActivityLogger events; falls back to DEFAULT_* constants when no events. Genuine statistical logic (frequenc |
| Anomaly persistence & REST (get active/history, resolve) | ✅ complete | src/server/routes/anomaly.ts -> DatabaseManager.getActiveAnomalies/getAnomalies/resolveAnomaly (tenant-aware) backed by real SQLite AnomalyRepository (INSERT/SELECT, src/database/repositories/AnomalyRepository.ts). |
| Pipeline decision tracing (PipelineTracer + /admin/monitoring/decision-traces) | ✅ complete | src/observability/PipelineTracer.ts builds real timing spans from MessageBus pipeline events (registered in src/pipeline/createPipeline.ts:96-97), keeps 100-trace ring buffer, computes stage averages/error rate (getStats). Exposed via src/server/routes/admin/monitoring.ts:452 decision-traces -> trac |
| Trace export (Console/JsonFile/OTLP exporters) | ✅ complete | src/observability/TraceExporter.ts implements ConsoleExporter, JsonFileExporter (NDJSON append), and OtlpExporter (real fetch POST to /v1/traces in OTLP JSON) with TraceExportManager.attach + createExporters() env-gated factory (TRACE_LOG_FILE, OTEL_EXPORTER_OTLP_ENDPOINT). Functional, no mocks. |
| Cost analytics (/api/monitoring/costs, cost trend chart) | ✅ complete | src/server/routes/monitoring.ts -> CostAnalyticsService (src/server/services/CostAnalyticsService.ts) persists CostRecords to data/cost-analytics.json with per-model PRICING_MAP, getHistoricalCosts/getDailyCosts aggregation. recordCost is wired from real flow: TokenBudgetService.ts:118-119. |
| Dashboard alert acknowledge/resolve (/api/dashboard/alerts/:id/*) | ✅ complete | src/server/routes/dashboard.ts:276-311 -> WebSocketService.acknowledgeAlert/resolveAlert; alerts are real AlertEvents recorded via WebSocketService.recordAlert (used by AnomalyDetectionService.runDetection and BroadcastService error handler). |
| Dashboard status / config-status / stats summary (/api/dashboard/*) | ✅ complete | src/server/services/DashboardService.ts getStatus()/getConfigStatus() read real BotConfigurationManager bots, WebSocketService.getBotStats message/error counts, provider connection probes (isProviderConnected lines 189-209), and demo-mode flag. |
| Live monitoring dashboard (CPU/memory/message-rate/error-rate charts, alerts, event stream) | 🟡 partial | src/client/src/pages/MonitoringDashboard.tsx renders MetricChart/AlertPanel/EventStream from useWebSocket() performanceMetrics+alerts. Metrics themselves are REAL (src/server/services/websocket/broadcast/MetricCalculator.ts computes process.cpuUsage/os mem and messageRate from event history). But me |
| MetricsCollector (singleton counters: messages/errors/tokens/responseTime) | 🟡 partial | src/monitoring/MetricsCollector.ts is real and fed by LLM token usage (src/llm/getLlmProvider.ts:44, taskLlmRouter.ts:67 call recordLlmTokenUsage). But incrementMessages/recordResponseTime are only called from src/services/demo/DemoActivitySimulator.ts; collectSystemMetrics() uses getRandomFloat() f |
| Analytics dashboard (AnalyticsDashboard page) | 🟡 partial | src/client/src/pages/AnalyticsDashboard.tsx fetches apiService.getActivity + getCostAnalytics and renders message volume / cost / live response-time. Backed by real services, but data emptiness is inherited from the broken activity-feed wiring; cost trend is real only if TokenBudgetService records c |
| Anomaly detection service (z-score, /api/anomalies, /api/admin/monitoring/anomalies) | 🟡 partial | src/services/AnomalyDetectionService.ts implements real rolling-window z-score detection, persists to SQLite (AnomalyRepository) and broadcasts WS alerts. Fed by PipelineTracer.completeTrace (PipelineTracer.ts:404-411) via addDataPoint('responseTime'/'errors'). BUT .start() (the 30s periodic runDete |
| Live orchestration log (pipeline_decision WS stream in ActivityPage) | 🟡 partial | src/client/src/pages/ActivityPage.tsx:342-355 subscribes to socket 'pipeline_decision'. Renders shouldReply/reason/probabilityRoll. Real-time only if the server emits pipeline_decision events; UI handles empty 'Waiting for orchestration events' gracefully. |
| Message Flow Replay / pipeline step reconstruction (ActivityPage drawer) | 🟡 partial | src/client/src/pages/ActivityPage.tsx:50-220 reconstructPipelineSteps INFERS the 7 pipeline steps from top-level event fields with hardcoded estimates (estimatedBaseChance=0.3, estimatedFinalProb, fabricated modifiers) — comment line 46-49 admits 'server does not persist per-step telemetry yet'. Not |
| Health alerts / error-pattern / recovery diagnostics endpoints (/health/alerts,errors,recovery) | 🟡 partial | src/server/routes/health/diagnostics.ts: /alerts derives from real process memory/uptime; /errors and /recovery use real ErrorLogger + globalRecoveryManager circuit-breaker stats. But /errors/patterns spikes/correlations/anomalies come from helpers (detectErrorSpikes/Correlations/Anomalies) — heuris |
| Standalone HealthChecker class (src/monitoring/HealthChecker.ts) | 🔲 stub | HealthChecker.ts checkDatabase()/checkServices() are placeholders (setTimeout sleeps, comments 'Placeholder for actual database check'); collectMetrics() returns getRandomFloat() for cpuUsage/diskUsage/activeConnections/errorRate (lines 192-214). Also NOT wired into any route — grep shows it's refer |
| AlertManager (threshold-based alerting + notification channels) | 🔲 stub | src/monitoring/AlertManager.ts is a full implementation but is driven by HealthChecker (whose metrics are random placeholders) and is NOT instantiated anywhere in production code (only console channel; no email/webhook/slack channels registered). Not wired into routes/services — dead alerting path.  |
| Activity REST API (/api/activity/messages,llm-usage,summary,agents,mcp-tools,chart-data) | 🔲 stub | src/server/routes/activity.ts: /messages and /llm-usage return empty arrays (lines 77,105); /chart-data fabricates time series with getRandomInt (lines 182-193); /agents and /mcp-tools return hardcoded sample objects (lines 223-278); /summary uses dbManager.getStats() but hardcodes averageResponseTi |
| IntegrationAnomalyDetector (per-integration Discord/Slack/Mattermost/LLM thresholds) | 📋 planned | src/monitoring/IntegrationAnomalyDetector.ts is a complete 670-line implementation but is referenced ONLY by ShutdownCoordinator; never instantiated/started in production and no route exposes it. detectForMcp/detectForDatabase just emit empty events (lines 428-437). Depends on ProviderMetricsCollect |
| ProviderMetricsCollector (per-provider message/LLM metrics + Prometheus) | 📋 planned | src/monitoring/ProviderMetricsCollector.ts is real (prom-client gauges/counters, latency percentiles) but its record* methods (recordMessageReceived/Sent/Failure/recordLlmRequest) are never called from message or LLM flow — grep finds callers only in IntegrationAnomalyDetector and ShutdownCoordinato |
| BusinessKpiCollector (engagement/performance/revenue/growth/retention KPIs) | 📋 planned | src/monitoring/BusinessKpiCollector.ts defines 13 default KPIs with aggregation logic, but grep shows zero usages outside its own file — not instantiated, fed, or exposed by any route. Pure scaffolding. |
| Activity feed (ActivityPage, /api/dashboard/activity, /api/activity/*) | ❌ broken | src/client/src/pages/ActivityPage.tsx + DashboardService.getActivity (src/server/services/DashboardService.ts:211) read real events from ActivityLogger (JSONL file). But ActivityLogger.log() is only called by DemoActivitySimulator. The real pipeline's flow recording (BroadcastService.setupMessageBus |

## auth-security

| Feature | Status | Evidence |
|---|---|---|
| JWT login / password authentication | ✅ complete | src/auth/AuthManager.ts:276-309 login() verifies bcrypt hash (12 rounds, AuthManager.ts:27) and issues 1h access + 7d refresh JWTs; wired at src/server/routes/auth.ts:51-68 POST /login with authRateLimiter + Zod validation. Production refuses to boot without JWT_SECRET/ADMIN_PASSWORD (AuthManager.ts |
| JWT verification middleware (authenticate) | ✅ complete | src/auth/middleware.ts:36-174 verifies Bearer token, loads user, attaches user+permissions. Duplicate impl in src/server/middleware/auth.ts:25-66 (authenticateToken). Both used across routes. |
| RBAC role hierarchy & permission checks | ✅ complete | src/auth/AuthManager.ts:30-52 rolePermissions for admin/bot-manager/user/viewer; src/auth/middleware.ts:180-248 requireRole (numeric hierarchy 197-200) and requirePermission; getUserPermissions/hasPermission AuthManager.ts:414-424. Enforced on auth and plugin-security routes. |
| Password change with old-password verification | ✅ complete | src/server/routes/auth.ts:362-399 verifies current password via authManager.verifyCurrentPassword (AuthManager.ts:534-538) before changePassword (AuthManager.ts:519-529). Note redundant duplicated isPasswordValid check at auth.ts:378-383 (harmless). |
| Localhost auth bypass / trusted-network admin login | ✅ complete | src/auth/middleware.ts:36-174 bypass gated behind ALLOW_LOCALHOST_ADMIN with strict IP+Host+Origin localhost checks (DNS-rebind/host-injection hardened, lines 43-97). src/server/middleware/security.ts:116-141 isTrustedAdminIP + ADMIN_IP_WHITELIST; passwordless src/auth/AuthManager.ts:315-330 trusted |
| Login UI (client) incl. trusted-network button | ✅ complete | src/client/src/components/Login.tsx:48-65 password login via useAuth().login; trustedLogin button (35-46,152-174) shown when isTrustedNetwork; serverless hint. Calls real backend endpoints. |
| Rate limiting (auth/api/admin/config/default tiers) | ✅ complete | src/middleware/rateLimiters.ts defines 5 limiters; src/middleware/rateLimiter.ts:174-206 applyRateLimiting routes by path, wired globally in src/server/setupMiddleware.ts:40. authRateLimiter keys on IP+username (rateLimiters.ts:43-47). Redis store in prod with in-memory fallback w/ TTL cleanup (rate |
| Proxy-aware client IP resolution & spoofing protection | ✅ complete | src/middleware/rateLimiterCore.ts:88-317 validateIP (rejects CRLF/header-injection line 92), CIDR checks, getTrustedProxies, and getClientKey that ignores X-Forwarded-For unless the direct connection is a trusted proxy (280-316). |
| Security headers + CSP (dev/prod) + HSTS | ✅ complete | src/server/middleware/security.ts:19-111 sets X-Frame-Options DENY, nosniff, Referrer-Policy, strict prod CSP (no unsafe-inline/eval, 71-87) vs relaxed dev CSP, HSTS only in prod over https, removes X-Powered-By. Wired globally in setupMiddleware.ts:87. |
| At-rest field encryption (EncryptionService AES-256-GCM) | ✅ complete | src/database/EncryptionService.ts:60-97 AES-256-GCM with random IV + authTag, 'enc:iv:tag:data' format, malformed-input guard (81-83). Key from DATABASE/ENCRYPTION_KEY config or config/.key; throws in production if absent (41-43); plaintext fallback with warning in dev (45-48). |
| Secure config manager (encrypted secret storage + backups) | ✅ complete | src/config/SecureConfigManager.ts AES-256-GCM encrypt/decrypt (523-548), checksum integrity verification (181-190,464-472), path-traversal guards (79-108,433-443), backup/restore (360-498), rotation-interval warning (195-204). Used by AuthManager to persist JWT secrets (AuthManager.ts:64-73,119-131) |
| Plugin manifest signing & verification (HMAC-SHA256) | ✅ complete | src/plugins/PluginSecurity.ts:78-118 canonicalize/sign/verify with timingSafeEqual constant-time compare; verifyAndSetTrust (215-265) sets trusted/untrusted and grants capabilities only on valid signature. Signing key from HIVEMIND_PLUGIN_SIGNING_KEY, required in prod (PluginManager.ts:426-441). |
| Plugin capability/trust policy + admin API | ✅ complete | src/plugins/PluginSecurity.ts:141-372 grants/revokes capabilities, audit events, built-in auto-trust (194-199). REST API src/server/routes/pluginSecurity.ts:69-238 (GET /security, POST /:name/verify, /:name/trust) guarded by authenticate+requireAdmin (16-18). Wired end-to-end; note auth skipped when |
| Plugin Security Dashboard (client) | ✅ complete | src/client/src/pages/PluginSecurityPage.tsx fetches /api/admin/plugins/security (60), real verify/trust actions (75-120), stats/filter/pagination. Renders real backend data, not mocks. |
| Admin IP whitelist guard on /api/* | ✅ complete | src/server/registerRoutes.ts:80-94 applies ipWhitelist (adminIPGuard, security.ts:146-160) to /api/* unless HTTP_ALLOW_ALL_IPS=true; defaults to loopback+RFC1918 ranges (rateLimiterCore.ts:224-235). |
| E2E test auth bypass (ALLOW_TEST_BYPASS) | ✅ complete | src/server/middleware/auth.ts:29-40 injects fake admin when ALLOW_TEST_BYPASS=true, with production refusal (30-37). Also honored in routes/auth.ts:249-260 and rate-limit skip (rateLimiterCore.ts:323-329). Intentional test affordance, production-guarded. |
| Refresh-token rotation & logout | 🟡 partial | src/auth/AuthManager.ts:335-375 rotates and revokes refresh tokens, but the allow-list is an in-memory Set (AuthManager.ts:24) — all sessions are invalidated on restart and not shared across instances/replicas. No persistent/Redis token store. |
| User registration / CRUD (users API) | 🟡 partial | src/server/routes/auth.ts:96-112,404-510 register/list/get/update/delete guarded by authenticate+requireAdmin. Backend works but the user store is a non-persistent in-memory Map (AuthManager.ts:20-22); only the default 'admin' survives restart. Registered users are lost on reboot. |
| Bot-specific rate limiting | 🟡 partial | src/middleware/rateLimiter.ts:111-168 createBotRateLimiter reads getBotRateLimitSettings(botName); only active when a guard profile enables it (118). Functional but depends on per-bot config and a fragile bot-name extraction from params/query/body/header (95-106). |
| Input sanitization middleware (XSS) | 🟡 partial | src/middleware/sanitizationMiddleware.ts:65-88 HTML-escapes body/query/params and x-* headers. Implementation is sound but it is NOT wired into the active app (no import in index.ts/registerRoutes/setupMiddleware). CSP security note (security.ts:46-48) cites it as the mitigation for unsafe-inline, b |
| CORS configuration | 🟡 partial | Active app uses inline localhost-only CORS in setupMiddleware.ts:46-68 (regex http://localhost|127.0.0.1). A more configurable secureCORS exists in security.ts:165-192 and corsMiddleware.ts but the live entry only allows localhost origins; the SettingsSecurity UI corsOrigins list is not consumed by  |
| Plugin install/update/uninstall (git clone) | 🟡 partial | src/plugins/PluginManager.ts:247-369 clones via execFileSync (no shell, args array), pnpm install --ignore-scripts (276,345), name path-traversal guard (180-184), runs signature verification post-install (282-283). Exposed at src/server/routes/marketplace.ts:381-411 with requireRole('admin'). Works  |
| Plugin install SSRF / URL validation guard | 🟡 partial | src/plugins/PluginManager.ts:196-242 validateRepoUrl blocks non-http(s) protocols, leading-dash, spaces, shell metacharacters, and --flag injection. GAP: it does NOT block internal/loopback hosts (localhost, 127.0.0.1, 169.254.169.254 metadata, RFC1918, file://-equivalents) — an admin-triggered git  |
| Audit logging of security events | 🟡 partial | src/server/middleware/auditLogger.ts and audit.ts exist and PluginSecurityPolicy emits structured audit events (PluginSecurity.ts:169-183), but the SettingsSecurity 'audit logging' toggle is non-functional and there is no evidence the global audit middleware is wired into the active request pipeline |
| Security settings UI (2FA, lockout, audit logging, session timeout, API-key auth, security-headers toggle) | 🔲 stub | src/client/src/components/Settings/SettingsSecurity.tsx renders toggles for enableTwoFactor, maxLoginAttempts, lockoutDuration, sessionTimeout, enableAuditLogging, enableApiKeyAuth, enableSecurityHeaders, corsOrigins — but onSubmit (127-145) only persists auth.enabled and rateLimit.enabled/maxReques |
| Tenant isolation middleware | 🔲 stub | src/auth/middleware.ts:329-332 requireTenant is a pass-through with no validation ('Skip tenant validation for now - just pass through'). |
| Session manager with token rotation (SessionManager/SessionStore) | 📋 planned | src/auth/SessionManager.ts (createSession/rotateToken/validateSession/sessionMiddleware) and src/auth/SessionStore.ts (bounded in-memory store w/ TTL cleanup) are fully written but NOT referenced anywhere in the production app (no imports in index.ts/registerRoutes/setupMiddleware). The live app rel |
| Account lockout after failed login attempts | 📋 planned | UI exposes maxLoginAttempts/lockoutDuration (SettingsSecurity.tsx:28-29,48-49) but no server-side lockout logic exists; AuthManager.login (AuthManager.ts:276-309) has no attempt counter or lockout — only IP+username rate limiting via authRateLimiter approximates this. |
| Two-factor authentication (2FA) | 📋 planned | Only a UI toggle (SettingsSecurity.tsx:214-228, defaults false); no TOTP/secret/verification code anywhere in src/auth. Not implemented. |
| CSRF protection middleware | ❌ broken | src/server/middleware/csrf.ts implements token + double-submit patterns correctly (timingSafeEqual, httpOnly sameSite=strict cookies). BUT csrfProtection is only mounted in src/server/server.ts:146 (HivemindServer class), which is NOT the active entrypoint. The live app (src/index.ts:68 -> registerR |

## persistence

| Feature | Status | Evidence |
|---|---|---|
| SQLite database backend (better-sqlite3 wrapper) | ✅ complete | src/database/sqliteWrapper.ts implements IDatabase with run/all/get/exec/transaction/close, robust constructor handling, and applies production PRAGMAs (WAL, synchronous=NORMAL, foreign_keys=ON, busy_timeout, cache_size). Wired in DatabaseManager.connect() lines 168-179. |
| Schema migrations (Umzug runner) | ✅ complete | src/database/migrationRunner.ts runs migrations via Umzug with a custom umzug_migrations storage table; src/database/migrations/000_initial_schema.ts creates ~20 tables (messages, bot_configurations, versions, audit, users, roles, tenants, audits, anomalies, memories w/ pgvector, etc.) + indexes for |
| Repositories (Message, BotConfig, Anomaly, Approval, AIFeedback, Decision, Activity, Inference, Memory) | ✅ complete | src/database/repositories/*.ts — 10 repositories with real SQL CRUD, all delegated through DatabaseManager (lines 252-260 init; methods 298-563). Includes vector memory search (MemoryRepository), config versioning & audit (BotConfigRepository.createBotConfigurationVersion/Audit). |
| At-rest field encryption (AES-256-GCM) | ✅ complete | src/database/EncryptionService.ts encrypts/decrypts sensitive fields with AES-256-GCM, key from DATABASE_ENCRYPTION_KEY or config/.key; format 'enc:iv:authTag:data'; fails hard in production if no key, warns otherwise. Used by BotConfigRepository (import line 2). |
| Data retention / cleanup (by date & row count) | ✅ complete | DatabaseManager.cleanupTableByDate/cleanupTableByRows/runFullCleanup (lines 628-729) with tiered retention limits (lite/cloud/standard) and AUTO_CLEANUP_ON_STARTUP trigger (lines 207-211). Real DELETE statements for both sqlite and postgres. |
| Factory reset (resetDatabase) | ✅ complete | DatabaseManager.resetDatabase() lines 569-622 truncates ~22 tables with FK checks disabled (PRAGMA foreign_keys=OFF / SET CONSTRAINTS DEFERRED). Exposed via admin maintenance /system/reset route (src/server/routes/admin/maintenance.ts:479). |
| Config backup creation (with retention + cold storage) | ✅ complete | src/server/services/configImportExport/backupManager.ts createBackup() exports all bot configs, writes <backup>.json[.enc].gz + .meta sidecar, enforces configurable backupRetentionLimit, optional cold-storage archival, and emits AuditLogger admin actions (lines 65-207). Wired via POST /api/import-ex |
| Backup list / delete / download | ✅ complete | backupManager.listBackups (reads .meta files, sorts newest-first), deleteBackup, getBackupFilePath with PathSecurityUtils (lines 239-327). Routes GET /backups, DELETE /backups/:id, GET /backups/:id/download (importExport.ts:382-514). Client BackupsPage fully wired via apiService.listSystemBackups/de |
| Scheduled automated backups (daily + retention) | ✅ complete | src/server/services/BackupSchedulerService.ts runs a 24h interval backup (first after 60s), applies RETENTION_COUNT=7 retention. Actually started at runtime: src/server/initServices.ts:343-344 BackupSchedulerService.getInstance().start() with shutdown hook. |
| Backup/export encryption & gzip compression | ✅ complete | src/server/services/configImportExport/cryptoUtils.ts encryptData/decryptData (AES-256-GCM, scrypt-derived key, salt+iv+authTag layout), compressData/decompressData (gzip), calculateChecksum (sha256). Used throughout ConfigurationImportExportService export/import. |
| Config export to JSON | ✅ complete | ConfigurationImportExportService.exportConfigurations (lines 98-227) bulk-fetches configs/versions/templates/audit logs, JSON.stringify via convertFormat (line 728), optional encrypt+compress+checksum. Route POST /api/import-export/export (importExport.ts:233). |
| Config import from JSON | ✅ complete | ConfigurationImportExportService.importConfigurations (lines 418-673) parses JSON, validates via ConfigurationValidator, bulk-checks existing configs, creates/updates with overwrite handling, imports versions & templates with N+1-avoiding batching. Route POST /api/import-export/import + multer uploa |
| Import validation (validate-only, no DB write) | ✅ complete | POST /api/import-export/validate (importExport.ts:520) calls importConfigurations with validateOnly:true; service path honors validateOnly to collect errors without persisting (lines 502-521). Works for JSON. |
| Bot export/import (separate from config backup) | ✅ complete | src/server/routes/bots.ts GET /export (redacts sensitive fields via sanitizeBotForExport, adds schemaVersion) and POST /import (BotImportSchema validation + create/update/error report). Mounted at /api/bots (registerRoutes.ts:133). |
| Audit log persistence (file-based) | ✅ complete | src/common/auditLogger.ts is a real append-only JSON-lines audit.log writer with size-based rotation (5x10MB), async queue, EventEmitter for live consumers, filtering (buildFilter), pagination (getAuditEvents w/ circular buffer), and getAllMatchingEvents. logConfigChange/logBotAction/logAdminAction  |
| DB-backed config audit/version trail | ✅ complete | bot_configuration_audit & bot_configuration_versions tables (migrations/000_initial_schema.ts:117-157) with BotConfigRepository.createBotConfigurationAudit/Version/getBotConfigurationAuditBulk; included in exports (ConfigurationImportExportService lines 138-186). |
| Audit log CSV export (/api/enterprise/audit/export) | ✅ complete | enterprise.ts GET /audit/export (lines 245-304) is the one enterprise handler that actually returns data: pulls auditLogger.getAllMatchingEvents(filter), builds escaped CSV, sets Content-Disposition attachment, res.send(csv). |
| Client BackupsPage (backup management UI) | ✅ complete | src/client/src/pages/BackupsPage.tsx fully wired to real endpoints via apiService.listSystemBackups/createSystemBackup/restoreSystemBackup/deleteSystemBackup/downloadSystemBackup (api/admin.ts -> /api/import-export/*). Real create/restore/delete/download flows, encryption key handling, stats, retent |
| WebUI JSON file storage (agents/personas/mcp/providers/guards) | ✅ complete | src/storage/webUIStorage.ts persists WebUI config to config/user/webui-config.json with optional SecureConfigManager encryption, legacy-plaintext auto-migration, serialized save queue, and per-entity CRUD (agents, personas, mcpServers, llmProviders, messengerProviders, guards w/ default seeding). |
| QueryBuilder utility | ✅ complete | src/database/queryBuilder.ts (14KB) provides programmatic SQL building used by repositories; no stubs observed (supporting infra rather than a user-facing feature). |
| PostgreSQL database backend (pg Pool wrapper) | 🟡 partial | src/database/postgresWrapper.ts implements IDatabase with a Pool, ?->$n param translation, auto-appends 'RETURNING id' on INSERTs, and a snake_case->camelCase row mapper. Wired in DatabaseManager.connect() lines 180-195. Marked partial: translateSql is admittedly naive (comment lines 36-41 — no 'INS |
| Connection pool (alternative connect path) | 🟡 partial | src/database/connectionPool.ts ConnectionPool mirrors DatabaseManager's connect logic but only supports sqlite — postgres throws 'Database type not yet implemented' (lines 64-69). Not the primary path (DatabaseManager is); appears largely redundant/legacy. |
| Config backup restore | 🟡 partial | backupManager.restoreFromBackup() (lines 212-234) -> ConfigurationImportExportService.importConfigurations. Works for JSON backups. Marked partial because restore of YAML-format payloads is impossible (parseYAML throws, see below) and the restore overwrites configs but there is no DB-level transacti |
| Config export to YAML | 🟡 partial | src/server/services/configImportExport/formatConverters.ts convertToYAML (lines 27-57) is a hand-rolled, simplified serializer (comment: 'In production, use a proper YAML library') — no quoting/escaping, cannot round-trip (since parseYAML is unimplemented). Export produces output but it is lossy and |
| Config export to CSV | 🟡 partial | formatConverters.convertToCSV (lines 65-95) only serializes the flat top-level keys of the first configuration's shape; nested objects become '[object Object]'. Throws if data has no .configurations array. Simplified ('In production, use a proper CSV library'); lossy for nested config. |
| Config import from CSV | 🟡 partial | formatConverters.parseCSV (lines 112-143) does naive comma-split parsing (no embedded-newline/quoted-field handling beyond simple dequoting) producing flat string-valued config objects, which will mostly fail ConfigurationValidator and cannot reconstruct nested provider config. Functional only for t |
| Main config (default/development/production) export & import | 🟡 partial | ConfigurationImportExportService.exportMainConfig/importMainConfig (lines 232-407) use SecureConfigManager to encrypt/store env config files. Functional for JSON; inherits the YAML(broken)/CSV(partial) format gaps; importMainConfig writes directly to <env>.json.enc via private mainConfigDir bracket- |
| admin/backup.ts route module | 🔲 stub | src/server/routes/admin/backup.ts defines only an unused _configRateLimit and `export default router` with zero routes (lines 1-17). It is mounted (admin/index.ts:75) but contributes no endpoints. The functional backup routes are entirely in importExport.ts. |
| Enterprise compliance / cloud-providers / integrations / governance / performance APIs | 🔲 stub | src/server/routes/enterprise.ts — all of GET /compliance, /cloud-providers, POST /cloud-providers, /integrations, POST /integrations, /performance, POST /compliance/check, /security/alerts, /governance/policies, POST /performance/optimize build hardcoded mock objects into eslint-disabled unused vars |
| admin tool-usage-guards persistence | 🔲 stub | src/server/routes/admin/audit.ts GET/POST/PUT/DELETE/toggle for /tool-usage-guards return hardcoded mock guards and echo-back objects; comments 'In a real implementation, this would save to database' (lines 38-218). Listed here as it shares the audit route file within this domain's audit area. |
| Alternate DI schema system (SchemaManager + ConnectionManager + SchemaRegistry) | 🟡 partial | src/database/SchemaManager.ts + ConnectionManager.ts + schemas/* (13 schema modules, ISchemaModule interface) form a second, modular schema-creation system. It is intentionally NOT on the live connect/schema path — the live path remains DatabaseManager+runMigrations (which creates the messages/bot_configurations/etc. tables that the repositories read). Wiring this system live would CREATE-IF-NOT-EXISTS a parallel set of snake_case tables (bots/bot_configs/personas/sessions/...) that no repository reads, so it stays opt-in/on-demand. As of feat/planned-wire-schema-manager it is now functional and DI-resolvable: ConnectionManager exposes an IDatabase-compatible adapter (getSchemaDatabase) so the schema modules' async run/exec calls work, and registration.ts registers ConnectionManager + SchemaManager via databasePath-bearing factories (previously SchemaManager was unresolvable because tsyringe could not auto-construct ConnectionManager's required options). Covered by tests/database/SchemaManager.test.ts + SchemaManagerDI.test.ts. |
| DatabaseManager (central connection lifecycle + repository facade) | ❌ broken | src/database/DatabaseManager.ts is the live persistence entry point (configure/connect/disconnect, delegates to 9 repositories). However it has duplicate import statements (PostgresWrapper lines 8&22; ActivityRepository 9&16; MessageRepository 15&19) and imports a non-exported InferenceLog — confirm |
| Config import from YAML | ❌ broken | formatConverters.parseYAML (lines 102-106) throws new Error('YAML parsing not implemented in this version'). importConfigurations/importMainConfig select parseYAML for .yaml/.yml uploads (ConfigurationImportExportService lines 449-451, 376-378), so any YAML import fails outright despite YAML being a |
| Audit log query API (/api/enterprise/audit) | ❌ broken | src/server/routes/enterprise.ts GET /audit (lines 207-242) builds the filter and calls auditLogger.getAuditEvents into a variable named auditEvents but then returns res.json(ApiResponse.success()) with NO data — the result is discarded (the var is eslint-disabled as unused). Every enterprise GET han |
| Client AuditPage (audit log viewer) | ❌ broken | src/client/src/pages/AuditPage.tsx fetches '/api/admin/audit-logs?limit=100' (line 201). That endpoint is a hardcoded placeholder: src/server/routes/admin/audit.ts:221 `router.get('/audit-logs', (req,res)=>res.json(ApiResponse.success({logs:[]})))` — always returns empty. The real file-based AuditLo |
| Client ImportExportPage | ❌ broken | src/client/src/pages/ImportExportPage.tsx posts to '/api/admin/export' (line 76) and '/api/admin/import' (line 106). No such routes are registered under /api/admin (admin/index.ts mounts no export/import handler; admin/backup.ts is an empty router). The real config import/export lives at /api/import |

## webui-infra

| Feature | Status | Evidence |
|---|---|---|
| Server bootstrap (src/index.ts + registerRoutes) | ✅ complete | src/index.ts:68 calls registerRoutes(app, ...); src/server/registerRoutes.ts:72-162 mounts all API routers in a documented order (auth-first, resource routers, catch-all openapi last). This is the ACTUAL runtime route setup. NOTE: src/server/server.ts (WebUIServer class) is a parallel/legacy setup N |
| Feature flags (HTTP_ENABLED, ENABLE_VITE_DEV, etc.) | ✅ complete | src/index.ts:74 (httpEnabled = HTTP_ENABLED!=='false'), :116 (enableViteDev = ENABLE_VITE_DEV!=='false'); SKIP_MESSENGERS/WEBHOOK_ENABLED handled in initServices. Flags read and gate behavior; startup logs '🎉 Open Hivemind Server startup complete!' at index.ts:213. |
| Dependency Injection container (src/di) | ✅ complete | src/di/container.ts wraps tsyringe with TOKENS, register/resolve helpers; src/di/registration.ts registerServices() is invoked at startup (src/server/initServices.ts:13,245). Real and wired. |
| ProviderRegistry / SyncProviderRegistry (src/registries) | ✅ complete | src/registries/ProviderRegistry.ts is a full singleton registry (message/llm/memory/tool providers + installers), consumed across src/llm/getLlmProvider.ts, src/initProviders.ts, src/server/routes/config/store.ts, health/basic.ts. SyncProviderRegistry.ts provides startup-init + sync O(1) hot-path lo |
| MessageBus event system (src/events) | ✅ complete | src/events/MessageBus.ts: typed singleton bus with on/once/off/emit/emitAsync, error isolation, once-pruning. Consumed by the 5-stage pipeline (src/pipeline/*Stage.ts), inputProcessor, PipelineTracer. Fully implemented. |
| WebSocket service (real-time push) | ✅ complete | src/server/services/websocket/index.ts: tsyringe-injected WebSocketService split into ConnectionManager/BroadcastService/EventHandlers; initialized at src/index.ts:151-152 (wsService.initialize(server)); 5s metrics interval broadcasts bot status/system/monitoring. Records alerts, message flow, deliv |
| Client router / app shell (AppRouter) | ✅ complete | src/client/src/router/AppRouter.tsx: lazy-loaded routes, ProtectedRoute auth gate (useAuth), per-route ErrorBoundary, UberLayout/MainLayout, legacy redirects. All in-scope pages routed. |
| Marketplace backend (plugin discovery/install) | ✅ complete | src/server/routes/marketplace.ts: real impl — scanBuiltInPackages() reads packages/, searchGitHubPackages() queries GitHub topic API (5min cache), getInstalledPlugins() via PluginManager; install/uninstall/update call real installPlugin/uninstallPlugin/updatePlugin; community.json allowlist. Mounted |
| Marketplace page (UI) | ✅ complete | src/client/src/pages/MarketplacePage.tsx: fetches /api/marketplace/packages, install-from-URL modal, update/uninstall actions, search/type filter, community warning banner — all bound to the real backend. (Star ratings at line 40-45 are local-only state, cosmetic.) |
| Bot Templates page | ✅ complete | src/client/src/pages/BotTemplatesPage.tsx fetches /api/bot-config/templates; backend src/server/routes/botConfig.ts GET /templates returns a real template map (mounted /api/bot-config, registerRoutes.ts:134 — path matches). Use-template pre-populates create form via router state; copy-to-clipboard w |
| Response Profiles page + backend | ✅ complete | src/client/src/pages/ResponseProfilesPage.tsx full CRUD (create/edit/delete, swarm-mode picker, grouped settings) against /api/config/response-profiles; backend src/server/routes/config/providers.ts:482-560 uses responseProfileManager + broadcastConfigUpdate (WebSocket). Auto-refreshes on WS config  |
| Specs backend (persist/list/get) | ✅ complete | src/server/routes/specs.ts: real fs-backed store under specs/ with index.json; POST creates spec.md (path-traversal guarded), GET list + GET /:id with versions. Mounted /api/specs (registerRoutes.ts:125). |
| Developer page (tabbed: Sitemap/UI/Specs/Static) | ✅ complete | src/client/src/pages/DeveloperPage.tsx: Tabs with URL ?tab= sync, lazy-renders SitemapPage/DaisyUIShowcase/SpecsPage/StaticPagesPage. Router redirects /admin/{specs,sitemap,showcase,static} into the right tab (AppRouter.tsx:223-226). |
| About page | ✅ complete | src/client/src/pages/AboutPage.tsx: fetches /api/health for version/platform/node/status, AnnouncementBanner, GitHub links. Fully wired (read-only). |
| Help / FAQ page | ✅ complete | src/client/src/pages/HelpPage.tsx: static FAQ accordions + keyboard-shortcut reference. Intentionally backend-free; complete as a static info page (shortcuts listed match KeyboardShortcutsProvider). |
| Sitemap page + backend | ✅ complete | src/client/src/pages/SitemapPage.tsx consumes /sitemap.json (+ access filter), groups URLs by category, XML/JSON/HTML download links; backend src/server/routes/sitemap.ts builds real XML/JSON via 'sitemap' lib from route definitions, mounted at root (registerRoutes.ts:164). |
| Export page (backups + config/OpenAPI export) | ✅ complete | src/client/src/pages/ExportPage.tsx: list/create/delete/restore/download backups via apiService backup methods, exportConfig() download (json/csv/yaml), OpenAPI spec download via /webui/api/openapi.{json,yaml}. All bound to real apiService endpoints. |
| Health endpoints | ✅ complete | src/server/routes/health/{basic,detailed,diagnostics,metrics}.ts: basic.ts checks real DatabaseManager.isConnected() and iterates ProviderRegistry memory providers' healthCheck(); mounted at /api/health and /health (registerRoutes.ts:72-73). |
| API docs route introspection backend | ✅ complete | src/server/routes/apiDocs.ts GET / calls introspectRoutes(app) (src/server/utils/routeIntrospection) to build a live route catalog grouped by prefix; mounted /api/docs before the openapi catch-all (registerRoutes.ts:156). Backend is sound (the breakage is the client page's duplicate import). |
| Command palette (Ctrl+K) | 🟡 partial | src/client/src/components/CommandPalette.tsx: full keyboard-driven nav palette (filter, arrow nav, focus trap) wired via KeyboardShortcutsProvider.tsx. Gap: PALETTE_ITEMS (lines 17-30) is a hardcoded static list of ~12 destinations using stale legacy paths (e.g. '/admin/providers/llm', '/admin/monit |
| Specs page (list/view) + Spec creation UI | 🟡 partial | src/client/src/pages/SpecsPage.tsx list/search/paginate/view are real (useSpecs -> /api/specs; navigates to SpecDetailPage). BUT 'Add Specification' / 'Create Specification' buttons call handleNotImplemented() -> infoToast('Coming Soon') (lines 49-51,93,161) even though backend POST /api/specs exist |
| Hot reload backend (config apply/history/rollback) | 🟡 partial | src/server/routes/hotReload.ts is real (HotReloadManager applyConfigurationChange/getChangeHistory/getAvailableRollbacks/rollbackToSnapshot + WS alert) mounted at /api/hot-reload (registerRoutes.ts:136). GAP: the only UI consumer (BotConfigurationPage.tsx:224,258 — out of scope page) calls /api/conf |
| Webhook scheduled messages | 🟡 partial | src/server/routes/webhooks.ts:43-103: in-memory Map CRUD for scheduled messages (create/list/get/delete) with auth+role guards. Works as an API but is purely in-memory ('would be DB in production', line 17) and there is no scheduler that actually delivers the scheduled messages — status stays 'pendi |
| Webhook event recording (ingress -> event log) | 🔲 stub | src/server/routes/webhookEvents.ts exports recordWebhookEvent() to feed the in-memory event store, but grep shows ZERO callers anywhere in src outside the file itself. No webhook ingress point records events, so even the richer store would always be empty. Retry handler only 'simulates a 202 Accepte |
| CI/CD deployment routes | 🔲 stub | src/server/routes/ci.ts (mounted /api/ci, registerRoutes.ts:137): every endpoint (deployments list/create/get/rollback, drift, validate, pipeline status, tests run/results) builds a mock object then returns ApiResponse.success() with NO payload (e.g. lines 63,112,153,207,244,271,306,356). Comments s |
| API Docs page (auto-generated catalog + live test) | ❌ broken | src/client/src/pages/ApiDocsPage.tsx duplicate-imports Button on BOTH line 5 and line 13 ('import Button from ../components/DaisyUI/Button') — a TS2300 duplicate-identifier error that breaks the module build/load. Underlying design is otherwise complete: backend src/server/routes/apiDocs.ts introspe |
| Webhook events backend (list/detail/retry) | ❌ broken | Path/shape mismatch: WebhookEventsPage.tsx calls GET /api/webhooks/events (expects {data:{items,total,...}}), GET /api/webhooks/events/:id, and POST /api/webhooks/events/:id/retry. registerRoutes.ts:152 mounts src/server/routes/webhooks.ts at /api/webhooks, which returns {events:[],count:0} (wrong s |
