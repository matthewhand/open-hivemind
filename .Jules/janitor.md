## 2026-03-26 - Fix DI resolution for StartupGreetingService in SlackService | **Learning:** When dynamically requiring `tsyringe` managed services (e.g., `require('@src/services/StartupGreetingService').default`), you must resolve the class through the DI container before calling instance methods to avoid `is not a function` errors. | **Action:** Updated `SlackService.ts` to use `container.resolve` for `StartupGreetingService`.
