import Debug from "debug";
export default class DiscordMessage {
    constructor(content: string, author: string);
    public content: string;
    public author: string;
    // Add other properties and methods used in your project
}
