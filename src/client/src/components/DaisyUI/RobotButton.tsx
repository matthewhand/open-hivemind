import React from 'react';

export const RobotButton = () => <button>Button</button>;
export const RobotButtonGroup = ({ children }: { children: React.ReactNode }) => <div>{children}</div>;
export default RobotButton;
