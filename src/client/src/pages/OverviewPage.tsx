import React from 'react';
// Temporarily disabled due to missing dependencies from incomplete merge in main
// import UnifiedDashboard from '../components/UnifiedDashboard';

const OverviewPage: React.FC = () => {
  return (
    <div className="flex flex-col items-center justify-center min-h-[400px] text-center">
      <h2 className="text-2xl font-bold mb-4">Dashboard Under Maintenance</h2>
      <p className="text-base-content/60">This page is currently being rebuilt and will return shortly.</p>
    </div>
  );
};

export default OverviewPage;