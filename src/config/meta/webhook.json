{
  "group": "webhook",
  "keys": {
    "WEBHOOK_ENABLED": { "level": "advanced", "doc": "Enable webhook service" },
    "WEBHOOK_URL": { "level": "advanced" },
    "WEBHOOK_TOKEN": { "level": "advanced", "doc": "Shared secret for incoming requests" },
    "WEBHOOK_IP_WHITELIST": { "level": "advanced" },
    "WEBHOOK_PORT": { "level": "advanced" }
  }
}

