name: Deployments

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened, synchronize]
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run lint
        run: npm run lint

      - name: Check types
        run: npm run check-types

      - name: Build
        run: npm run build

      - name: Run quick smoke tests
        run: |
          # Quick smoke test to make sure basic imports work
          NODE_ENV=production node -e "
            try {
              require('./dist/src/index.js');
              console.log('✅ Build successful - basic imports work');
              process.exit(0);
            } catch (error) {
              console.error('❌ Build failed - import error:', error.message);
              process.exit(1);
            }
          "

  # Production deployments for main branch
  deploy-fly:
    if: github.ref == 'refs/heads/main'
    needs: ci
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - run: flyctl deploy -c fly.toml --wait-timeout 300
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

  deploy-vercel:
    if: github.ref == 'refs/heads/main'
    needs: ci
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Vercel Deployment Skipped
        run: |
          echo "✅ Vercel deployment skipped - already handled by Vercel GitHub Integration"
          echo "🚀 View live deployment at: https://open-hivemind.vercel.app"

  deploy-netlify:
    if: github.ref == 'refs/heads/main'
    needs: ci
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Netlify Deployment Skipped (Requires Configuration)
        run: |
          echo "⚠️ Netlify deployment skipped - requires NETLIFY_AUTH_TOKEN and NETLIFY_SITE_ID secrets"
          echo "📝 To enable: Add secrets to GitHub repository settings"
=======
      - name: Netlify Deployment Skipped (Requires Configuration)
        run: |
          echo "⚠️ Netlify deployment skipped - requires NETLIFY_AUTH_TOKEN and NETLIFY_SITE_ID secrets"
          echo "📝 To enable: Add secrets to GitHub repository settings"
>>>>>>> main
