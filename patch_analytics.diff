<<<<<<< SEARCH
    // Mock WebSocket Server via page.route WebSocket interception or inject mock io
    await page.addInitScript(() => {
      const now = Date.now();
      const mockMetrics = [];
      for (let i = 20; i >= 0; i--) {
        mockMetrics.push({
          timestamp: new Date(now - i * 5000).toISOString(),
          cpuUsage: 15 + Math.random() * 5,
          memoryUsage: 40 + Math.random() * 2,
          activeConnections: 10 + Math.floor(Math.random() * 5),
          messageRate: 5 + Math.random(),
          errorRate: 0.1,
          responseTime: 120 + i * 5 + Math.random() * 10
        });
      }

      // Mock the global io function from socket.io-client
      (window as any).io = () => {
        return {
          on: (event: string, callback: any) => {
            if (event === 'performance_metrics_update') {
              setTimeout(() => callback(mockMetrics), 100);
            }
          },
          disconnect: () => {},
          emit: () => {}
        };
      };
    });
=======
    // Mock WebSocket for performance metrics
    await page.routeWebSocket(/.*/, ws => {
      ws.onMessage(message => {
        // Handle engine.io connection setup and pings
        if (message === '2probe') ws.send('3probe');
        if (message === '5') ws.send('6');
      });

      // Send socket.io connection acceptance
      ws.send('0{"sid":"mock-session-id","upgrades":[],"pingInterval":25000,"pingTimeout":20000,"maxPayload":1000000}');
      ws.send('40'); // socket.io connect namespace

      // Send performance metrics
      const now = Date.now();
      const mockMetrics = [];
      for (let i = 20; i >= 0; i--) {
        mockMetrics.push({
          timestamp: new Date(now - i * 5000).toISOString(),
          cpuUsage: 15 + Math.random() * 5,
          memoryUsage: 40 + Math.random() * 2,
          activeConnections: 10 + Math.floor(Math.random() * 5),
          messageRate: 5 + Math.random(),
          errorRate: 0.1,
          responseTime: 120 + i * 5 + Math.random() * 10 // Trend down
        });
      }

      // Delay to ensure the client is ready
      setTimeout(() => {
        ws.send(`42["performance_metrics_update",${JSON.stringify(mockMetrics)}]`);
      }, 500);
    });
>>>>>>> REPLACE
