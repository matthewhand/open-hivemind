## 2026-05-06 - Encryption Tests Failing due to Environment Variable Override | **Learning:** Tests expecting encrypted outputs (e.g., `enc:` prefix) from `EncryptionService` fail globally because the test suite runs with `DISABLE_ENCRYPTION=true`. | **Action:** Mock the encryption key in `beforeAll` (e.g., `(encryptionService as any).encryptionKey = crypto.randomBytes(32)`) and clean it up in `afterAll` to bypass the environment limitation and ensure the tests pass.
