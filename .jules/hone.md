## 2024-06-14 | [Architectural Audit] | Insight: "Div-soup" vs Semantic Buttons | Protocol: Replace interactive `div role="button"` elements with semantic `<button type="button">` to ensure native keyboard operability (Space/Enter) and focus management out-of-the-box.
## 2024-06-14 | [Architectural Audit] | Insight: Empty State Accessibility | Protocol: Wrap empty state visualizations (like "No agents yet") in `<section aria-labelledby="...">` instead of generic `div`s to provide meaningful landmark navigation for screen readers.
## 2024-06-14 | [Architectural Audit] | Insight: Brittle Async State Logic | Protocol: Replace loose boolean flags (`loading`, `error`) in components like `AIAssistButton` with deterministic state machine variables (e.g., `state: 'idle' | 'loading' | 'success' | 'error'`) to prevent impossible states and UI race conditions.
