## 2025-01-20 | [Architectural Audit] | Insight: Div-soup interaction blocking | Protocol: Use semantic interaction buttons inside semantic row layouts and utilize e.stopPropagation inside the semantic interactive element (the action button) instead of an outer generic non-semantic div wrapper that breaks a11y focus routing.
## 2025-01-20 | [Architectural Audit] | Insight: Indeterminate loading states | Protocol: Replaced multiple competing boolean flags (`isLoading`, `abortController`) with a deterministic state machine (`fetchState`) to ensure explicit async states (`idle`, `testing`, `loadingAvatar`) and prevent impossible race conditions.
## 2025-01-20 | [Architectural Audit] | Insight: Full-page spinner jarring UX | Protocol: Replace generic `<LoadingSpinner />` full-page takeovers with context-aware semantic skeletons like `<SkeletonTimeline />` inside an `aria-live` container to preserve spatial context and improve screen-reader notifications.
## 2025-01-20 | [Architectural Audit] | Insight: Div-soup grouped fields | Protocol: Refactor generic nested `<div className="form-control">` groups into semantic `<fieldset>` structures paired with a `<legend>` to accurately inform assistive technology of the field groupings.
