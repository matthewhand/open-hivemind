## 2024-07-04 | [Architectural Audit] | Insight: "Div-soup" vs Semantic Fieldsets | Protocol: Use semantic HTML5 `<fieldset>` and `<legend>` for grouping form elements instead of raw `<div>` tags to improve screen reader context.
## 2024-07-04 | [Architectural Audit] | Insight: State Inconsistency in Async Buttons | Protocol: Use deterministic state machine variables (e.g., fetchState: 'idle' | 'testing' | 'loadingAvatar' | 'success' | 'error') for async network actions and couple them with robust aria-live announcements.
## 2024-07-04 | [Architectural Audit] | Insight: Missing Keyboard Focus Visibility | Protocol: Standardize keyboard focus states across all interactive elements (links, inputs, dropdown items, selects) using Tailwind's `focus-visible` utility classes to guarantee 100% keyboard operability.
