## 2025-03-09 | [Architectural Audit] | Insight: Div-soup Interaction & Event Bubbling | Protocol: Replace non-semantic <div> elements acting as interactive triggers with strictly defined semantic <button type="button"> elements. Scope `stopPropagation()` strictly within the button's onClick handlers instead of parent non-interactive wrappers to maintain reliable DOM event flows without sacrificing keyboard accessibility.
## 2025-03-09 | [Architectural Audit] | Insight: Unannounced Empty States | Protocol: Refactor EmptyState components to use <section> tags grouped with an `aria-labelledby` property tied to their internal visible heading rather than isolated <div> elements. Combine with `aria-live` to guarantee screen-reader discovery in async loading scenarios.
## 2025-03-09 | [Architectural Audit] | Insight: Unfocusable Custom Triggers | Protocol: Any interactive custom button elements, such as close triggers or layout toggles (like in Modals and DetailDrawers) must strictly enforce focus visibility using Tailwind's `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:outline-none`.
